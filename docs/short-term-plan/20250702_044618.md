---
id: codex_task_20250702_044618
author: codex
date: 2025-07-02T04:46:18+00:00
model_context: "python3.10"
task_type: feature
priority: P1
linked_issues: []
---

## 1. Problem Statement

Ensure conversation history is restored when the `ConversationAgentWrapper` is created with a `MemoryManagerAgent`.

## 2. Input Context

* **파일/모듈**: `conversation_agent/orchestrator_wrapper.py`
* **테스트**: `tests/conversation_agent/test_history_methods.py`
* **문서**: `docs/conversation-agent.md`

## 3. Expected Output / Success Criteria

| # | 성공 조건 | 검증 방법 |
| - | ----- | -------- |
| 1 | Wrapper loads previous history on init if memory manager present | pytest |
| 2 | Unit tests updated accordingly | pytest |
| 3 | Docs updated in both languages | review |

## 4. Constraints

* Keep backwards compatibility for existing wrapper usage.

## 5. Step-by-Step Outline (for AI)

1. Modify `ConversationAgentWrapper.__init__` to accept optional `user_id` and call `load_history_from_memory` when a memory manager is provided.
2. Update or extend unit tests in `test_history_methods.py` to cover this behaviour using `MockMemoryManagerAgent`.
3. Update documentation to describe automatic history loading and provide Korean translation.
4. Run `pytest` and ensure all tests pass.
5. Commit changes with docs and tests.

## 7. Tests / Assertions

```python
wrapper = ConversationAgentWrapper(memory_manager=mm, user_id="u")
assert len(wrapper.agent.dialogue_manager.get_conversation_history()) > 0
```

## 8. Post-Generation Checks

* [ ] `pytest` 통과

## 9. References

* None

## 10. Revision History

| 버전 | 날짜 | 변경 내역 |
| --- | ------------ | ----- |
| 0.1 | 2025-07-02 | 최초 작성 |


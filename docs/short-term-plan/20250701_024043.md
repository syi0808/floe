---
id: codex_task_20250701_0240
author: Codex
date: 2025-07-01T02:40+00:00
model_context: "python3.10"
task_type: feature
priority: P1
linked_issues: []
---

## 1. Problem Statement

Enhance inbox agent to handle OAuth token refresh and action extraction reliably with unit tests.

## 2. Input Context

* **파일/모듈**: `inbox_agent/email_connectors.py`, `inbox_agent/email_processor.py`, `inbox_agent/inbox_agent.py`
* **데이터/API**: OpenAI chat completions

## 3. Expected Output / Success Criteria

| # | 성공 조건 | 검증 방법 |
| - | ----- | -------- |
| 1 | Token refresh helpers for Gmail and Outlook connectors | `pytest` unit tests |
| 2 | LLM-based action extraction implemented | unit tests using mocked OpenAI |
| 3 | `process_new_email` routes via `OrchestrationEngine` and stores summaries | unit tests |

## 4. Constraints

* Follow PEP8, type hints when modifying code

## 5. Step-by-Step Outline (for AI)

1. Implement `_ensure_token` and `_refresh_token` logic with 401 retry
2. Add `parse_email_actions` using OpenAI call wrapped in try/except
3. Update `process_new_email` to store summary via MemoryManager and route actions
4. Write unit tests for token refresh and action parsing
5. Run `pytest`

## 7. Tests / Assertions

```python
pytest -q
```

## 8. Post-Generation Checks

* [ ] `pytest` 통과
* [ ] `ruff` / `black` 포맷팅 확인
* [ ] `mypy` 타입 검사 통과

## 10. Revision History

| 버전 | 날짜 | 변경 내역 |
| --- | --- | --- |
| 0.1 | 2025-07-01 | 최초 작성 |

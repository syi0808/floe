---
id: codex_task_20250701_0237
author: codex
date: 2025-07-01T02:37:37+00:00
model_context: "python3.10"
task_type: feature
priority: P1
linked_issues: []
---

## 1. Problem Statement

Implement daily and weekly insight report generation methods with MCP notifications.

## 2. Input Context

* **파일/모듈**: `insight_agent/insight_generator.py`
* **데이터/API**: `MCPClient` notification endpoint

## 3. Expected Output / Success Criteria

| # | 성공 조건 | 검증 방법 |
| - | ----- | -------- |
| 1 | New methods `generate_daily_report` and `generate_weekly_report` produce correct summaries | `pytest` |
| 2 | MCP notifications dispatched when enabled | unit tests |
| 3 | Documentation updated | review |

## 4. Constraints

* Python 3.10+
* Follow existing coding style and type hints

## 5. Step‑by‑Step Outline (for AI)

1. Extend `InsightGenerator` with methods `generate_daily_report` and `generate_weekly_report` that wrap `generate_report` with appropriate period and send notifications.
2. Update `docs/insight-agent.md` to describe new usage.
3. Add tests verifying new methods produce expected output and dispatch notifications when provided an MCP client.
4. Run `pytest`.
5. Commit changes.

## 6. Code Skeleton *(필요 시)*

```python
class InsightGenerator:
    def generate_daily_report(...):
        ...

    def generate_weekly_report(...):
        ...
```

## 7. Tests / Assertions

```python
# tests/insight_agent/test_insight_generator.py
# - test_generate_daily_report
# - test_generate_weekly_report
```

## 8. Post‑Generation Checks

* [ ] `pytest` 통과
* [ ] `ruff` / `black` 포맷팅 확인
* [ ] `mypy` 타입 검사 통과

## 9. References

* docs/insight-agent.md

## 10. Revision History

| 버전 | 날짜 | 변경 내역 |
| --- | ------------ | --- |
| 0.1 | 2025-07-01 | 최초 작성 |

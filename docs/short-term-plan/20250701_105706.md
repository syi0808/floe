---
id: codex_task_20250701_105706
author: codex
date: 2025-07-01T10:57:06+00:00
model_context: "python3.10"
task_type: feature
priority: P1
linked_issues: []
---

## 1. Problem Statement

Persist InsightGenerator summaries across sessions using an optional disk-backed cache.

## 2. Input Context

* **파일/모듈**: `insight_agent/insight_generator.py`
* **테스트**: `tests/insight_agent/test_insight_generator.py`

## 3. Expected Output / Success Criteria

| # | 성공 조건 | 검증 방법 |
| - | ----- | -------- |
| 1 | Cache persists when cache_path is provided | pytest |
| 2 | JSON and SQLite paths both supported | pytest |

## 4. Constraints

* Use only standard libraries
* Preserve current behaviour when cache_path is None

## 5. Step-by-Step Outline (for AI)

1. Add `cache_path` parameter to `InsightGenerator.__init__` and load cache if present.
2. Implement JSON and SQLite handlers and flush after updates.
3. Update compile and clear_cache to persist changes.
4. Write tests verifying persistence across generator instances.
5. Document feature and run `pytest`.

## 7. Tests / Assertions

```python
path = tmp_path / "cache.json"
InsightGenerator(cache_path=str(path)).compile(sample_data())
assert path.exists()
```

## 8. Post-Generation Checks

* [ ] `pytest` 통과

## 9. References

* None

## 10. Revision History

| 버전 | 날짜 | 변경 내역 |
| --- | ------------ | ----- |
| 0.1 | 2025-07-01 | 최초 작성 |

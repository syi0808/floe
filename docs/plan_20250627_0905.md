# Development Plan 2025-06-27 09:05 UTC

Implement InboxAgent features and tests:
1. Add robust OAuth token refresh logic and helper methods in `inbox_agent/email_connectors.py`.
2. Introduce `parse_email_actions` function in `email_processor.py` using LLM to produce structured actions from email text.
3. Update `process_new_email` to leverage `parse_email_actions`, route actions via `OrchestrationEngine`, and store summaries and attachments through `MemoryManagerAgent`.
4. Write unit tests covering connector authentication refresh and action parsing.
5. Run `pytest` and commit changes.


# Plan 2025-06-30 02:35 UTC

## Goal
Extend `InsightGenerator` with daily/weekly report generation and MCP notifications. Update `InsightAgent` to expose report generation and add tests.

## Tasks
1. Review existing insight generator and agent implementation.
2. Expand `InsightGenerator.generate_report` to always support MCP notifications.
3. Add a `generate_report` helper in `InsightAgent` and update `process` to use it with period and notification options.
4. Create tests under `tests/insight_agent/` covering the new agent method and notification behaviour.
5. Run `pytest` to ensure all tests pass.

# Development Plan 2025-06-30 02:35 UTC

## Goal
Implement missing MCP client interactions for commands and notifications and test them.

## Tasks
1. Extend `mcp/client.py`:
   - Add a method `send_command` for POST `/mcp/commands`.
   - Ensure `_headers` raises a clear error when no token is configured.
   - Keep retry and error handling in `_request`.
2. Update integration tests under `tests/integration/`:
   - Verify that `send_command` and `send_notification` hit the correct endpoints and include the Authorization header.
   - Reuse `DummyResponse` helper from existing tests.
3. Run `pytest` to confirm all tests pass.
4. Document session plan and next steps.


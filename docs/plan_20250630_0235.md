# Development Plan 2025-06-30 02:35 UTC

Goal: Implement enhanced email processing features for InboxAgent.

Tasks:
1. Ensure `GmailConnector` and `OutlookConnector` correctly refresh OAuth tokens when expired and are usable as standalone helpers.
2. Extend `extract_email_actions` in `email_processor.py` to use LLM via `parse_email_actions` with keyword fallback.
3. Add tests verifying OAuth refresh logic and new action extraction behaviour in `tests/inbox_agent`.
4. Provide simple integration test covering `process_new_email` with orchestrator routing.
5. Run pytest (may fail due to missing deps) and commit changes.

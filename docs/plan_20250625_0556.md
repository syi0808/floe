# Development Plan 2025-06-25 05:56 UTC

Implement InboxAgent enhancements:
1. Add OAuth token refresh logic and helper methods in `GmailConnector` and `OutlookConnector`.
2. Extend `email_processor.py` with LLM-based action extraction. New function `parse_email_actions` will call OpenAI and return actions.
3. Update `process_new_email` to route extracted actions through `OrchestrationEngine` if supplied, falling back to direct agent calls. It should store summaries and attachments using `MemoryManagerAgent`.
4. Write unit tests covering new connector authentication calls, action parsing via mocked OpenAI, and orchestrator routing from `process_new_email`.


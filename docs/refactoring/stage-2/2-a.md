# 2-A — Compile and Test Ownership Restoration

**Status: complete**

## Goal
Restore a green, owner-aligned workspace after Stage 1 ownership moves without adding production dependencies merely to preserve stale tests.

## Checklist
- [x] classify ownership-split test failures by KEEP / REWRITE / MOVE / DELETE;
- [x] restore workspace library compilation;
- [x] move semantic assertions to owner modules;
- [x] preserve security failures instead of blanket allow;
- [x] remove helpers that duplicate production ownership;
- [x] keep architecture checker at zero errors/warnings;
- [x] avoid reverse production dependencies introduced only for tests.

## Reopen rule
Do not reopen 2-A for later runtime changes. Fix compile/test regressions inside the Stage 2 step that changes the contract.

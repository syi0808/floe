# 2-D — Internal Compatibility Cleanup

**Status: pending**

## Goal
Delete internal compatibility APIs after production callers move to canonical owners.

## Checklist
- [ ] inventory old Conversation `ModelRequest` / `ModelRunner` references;
- [ ] remove General Conversation `generate_with_recovery` compatibility after canonical Inference;
- [ ] remove `CapabilityHost` after ContextToolService owns tools;
- [ ] remove legacy internal route/profile values with zero production callers;
- [ ] remove model/capability conversion helpers;
- [ ] remove transitional budget helpers after Inference owns attempts;
- [ ] delete or relocate fixture-only entry points;
- [ ] collapse stale aliases/error mappings serving old paths.

## Guardrails
Outer FFI/Flutter DTO compatibility belongs to Stage 3. Do not replace deleted internal compatibility with renamed equivalents.

## Acceptance
- [ ] zero production references before each deletion;
- [ ] workspace compile/test green;
- [ ] architecture checker green;
- [ ] `git diff --check` clean.

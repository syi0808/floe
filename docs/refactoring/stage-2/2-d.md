# 2-D — Internal Compatibility Cleanup

**Status: ready**

**Code baseline reviewed:** 065910932ee99c200e7b87c0d334cd431568c4fe

This document is the authoritative execution plan for 2-D.

2-C is complete/frozen. Do not reopen 2-B/2-C or pull Stage 3-A Expert/domain convergence forward merely because a symbol looks legacy.

## Goal

Delete internal compatibility surfaces whose production caller count is now zero, while preserving compatibility that still serves an intentionally deferred production caller.

2-D is therefore:

~~~text
zero-production-caller compatibility deletion
+ fallout cleanup caused by those deletions
~~~

It is not:

~~~text
delete every old Conversation / ModelTransport symbol
canonicalize delegated Expert/Schedule runtime
outer FFI/Flutter compatibility cleanup
public-API minimization for its own sake
~~~

At 2-D exit, the known caller-less bridge leaves below are gone, no renamed equivalent replaces them, all still-live Stage 3-A/Stage 3 compatibility remains intact, and Stage 2 can move to 2-E public-surface closure.

## Fixed scope rule

Before deleting a compatibility symbol, classify it by actual current caller:

| Class | 2-D action |
| --- | --- |
| production caller = 0, compatibility-only | delete in 2-D |
| delegated Expert/Schedule production caller exists | keep for Stage 3-A |
| outer FFI/Flutter/remote-authority product boundary caller exists | keep for Stage 3 |
| only visibility/re-export ergonomics remain after behavior is retained | leave to 2-E unless deleting the underlying dead symbol makes the export disappear naturally |

Do not use names such as `legacy`, `compat`, `old` or `RemoteRoute` as deletion evidence. Caller ownership is the evidence.

## Revalidated current inventory

### Delete in 2-D

The current main call graph shows these surfaces have no live production caller outside their own definition/re-export/test chain.

#### Experts orphan

- `floe_experts::delegate_expert_task`
  - definition: `crates/modules/experts/src/dispatch.rs`;
  - re-export: `crates/modules/experts/src/lib.rs`;
  - its only prior caller chain was removed by 2-C.

#### Removed pre-turn remote model-resolution chain

The canonical General Conversation path no longer performs pre-turn remote route discovery.

Delete the now-orphaned resolver chain:

- `floe_inference::RemoteRouteResolver`;
- `floe_inference::select_remote_route`;
- `floe_provider_adapters::models::RemoteModelRouteResolver`;
- `floe_provider_adapters::models::ResolvedRemoteConnection`;
- `floe_provider_adapters::models::resolve_remote_model_route`;
- `floe_inference::plan_remote_route` once the resolver chain is removed and its caller count becomes zero.

Also delete code that only exists to support that chain, including:

- the route-selection unit tests that exercise `select_remote_route`;
- `ObservedConnectorCatalog` and connector-catalog projection imports in `models/server.rs` if they become unused;
- corresponding re-exports.

Keep `SavedConnectionStore`: current recipient authority and endpoint credential stores still use it.

#### Dead recipient-snapshot accessors

Delete:

- `RootModelProvider::consented_external_recipients`;
- `ServerModelProvider::consented_external_recipients`.

The canonical authority path already re-reads the saved connection on every Access check. Do not replace these methods with another copied recipient snapshot.

If `ServerModelProvider.external_recipients` becomes unused after deleting the accessor, delete that field and its constructor/fixture plumbing too. Keep `allow_external` wherever the canonical prepared server transport still requires it.

#### Dead single-attempt Conversation bridge helpers

Delete:

- `generate_once`;
- `validate_generate_once_request`;
- `dispatch_generate_once`;

from `crates/modules/conversation/src/turn/model_recovery.rs` and their re-exports from `turn/mod.rs`.

They were the single-attempt bridge for the removed General Conversation model compatibility path and currently have no caller.

Keep `generate_with_recovery`: delegated legacy Experts still execute through it until Stage 3-A.

#### Caller-less TransportModelRunner accessor

Delete `TransportModelRunner::transport()` if revalidation at execution time still shows zero callers.

Do not delete `TransportModelRunner` or `transport_request`; delegated Expert/Schedule compatibility still uses the runner.

### Keep until Stage 3-A

These are still live production compatibility and are explicitly out of 2-D:

- `ModelRequest`;
- `ModelResponse`;
- `ModelRunner`;
- `CapabilityHost`;
- `AgentRuntime`;
- `generate_with_recovery`;
- `TransportModelRunner`;
- `transport_request`;
- `UsageLedger` behavior used by that runtime;
- `crates/app/src/vault_host/conversation_turn/expert_compat.rs`;
- `crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule/agent.rs`;
- legacy Expert/Schedule `InferencePolicyDecision` usage;
- legacy Expert/domain `ModelTransport` execution.

These remain because delegated Expert/Schedule production callers still use them. Their convergence belongs to `docs/refactoring/stage-3/3-a.md`.

### Keep until Stage 3 product-boundary cleanup

Do not delete or redesign in 2-D:

- `RemoteTurnRoute`;
- `AgentRemoteRouteDto`;
- FFI/protocol remote-route conversion;
- remote authority/pairing worker operations;
- `AppHost::legacy_services()` / FFI outer compatibility solely because of its name;
- Flutter/native/server product-boundary compatibility.

Those are Stage 3 caller/wire concerns.

### Keep while still required by Expert/domain or outer callers

Do not wholesale delete:

- `SavedServerConnection`;
- `RemoteRoute`;
- `RoutePairing`;
- `RemoteModelConnection`;
- `ModelRouteConfig`;
- `candidate_route`;
- `LEGACY_INFERENCE_CONSUMER`;
- `MODEL_GENERATION_CAPABILITY`.

Some are still used by delegated Expert/domain transport or outer remote authority/pairing. 2-D may remove a dead helper around them, but not the live value merely to reduce grep output.

## Execution sequence

## D0 — Revalidate the deletion boundary

Primary references:

- `AGENTS.md`;
- `docs/refactoring/stage-2.md`;
- this file;
- `docs/refactoring/stage-3/3-a.md`.

Before editing code:

- [ ] confirm HEAD still descends from the reviewed 2-C closure;
- [ ] confirm 2-C remains complete/frozen;
- [ ] confirm each D1-D4 delete target still has zero production callers;
- [ ] if a target gained a real caller, classify that caller before changing anything;
- [ ] do not broaden the work to a general legacy-code audit.

Use repository search plus compiler feedback; do not infer callerlessness from naming.

## D1 — Remove the orphaned Experts delegation helper

Primary files:

- `crates/modules/experts/src/dispatch.rs`;
- `crates/modules/experts/src/lib.rs`.

Changes:

- [ ] delete `delegate_expert_task`;
- [ ] delete its public re-export;
- [ ] remove imports used only by that helper;
- [ ] do not change `TaskCoordinator`, `DelegationPort`, Task identity, Task lifecycle, cancellation or settlement.

Static gate:

~~~sh
rg -n "delegate_expert_task" crates --glob '*.rs'
~~~

Expected: no code references.

Focused validation:

~~~sh
cargo test -p floe-experts
cargo check -p floe-experts
~~~

## D2 — Delete the obsolete pre-turn remote route resolver chain

Primary files:

- `crates/modules/inference/src/application/route_selection.rs`;
- `crates/modules/inference/src/application/route_config.rs`;
- `crates/modules/inference/src/application/mod.rs`;
- `crates/modules/inference/src/lib.rs`;
- `crates/adapters/providers/src/models/server.rs`;
- `crates/adapters/providers/src/models/mod.rs`.

Changes:

- [ ] delete `RemoteRouteResolver`;
- [ ] delete `select_remote_route` and its unit tests;
- [ ] preserve `SavedConnectionStore`;
- [ ] delete `RemoteModelRouteResolver`;
- [ ] delete `ResolvedRemoteConnection`;
- [ ] delete `resolve_remote_model_route`;
- [ ] delete connector-catalog observation structs/imports used only by that resolver;
- [ ] delete `plan_remote_route` after proving the resolver removal leaves it caller-less;
- [ ] remove corresponding re-exports;
- [ ] keep `candidate_route`, `ModelRouteConfig`, `RemoteModelConnection`, `RemoteRoute`, `RoutePairing`, `PurposeAvailability` when still used by live Expert/domain/canonical code.

Required proof:

- canonical `RootModelProvider` still discovers only model purposes for model profile observation;
- source connector discovery remains lazy in source/Expert paths that still need it;
- no pre-turn General Conversation route resolution is reintroduced.

Static gates:

~~~sh
rg -n "RemoteRouteResolver|select_remote_route|RemoteModelRouteResolver|ResolvedRemoteConnection|resolve_remote_model_route|plan_remote_route" crates --glob '*.rs'
~~~

Expected: no code references to the deleted resolver chain.

Focused validation:

~~~sh
cargo test -p floe-inference
cargo test -p floe-provider-adapters
cargo check -p floe-inference
cargo check -p floe-provider-adapters
~~~

## D3 — Remove stale recipient snapshots from provider APIs

Primary files:

- `crates/adapters/providers/src/models/root.rs`;
- `crates/adapters/providers/src/models/server.rs`;
- provider tests affected only by these accessors.

Changes:

- [ ] delete `RootModelProvider::consented_external_recipients`;
- [ ] delete `ServerModelProvider::consented_external_recipients`;
- [ ] delete `ServerModelProvider.external_recipients` if it has no remaining use;
- [ ] simplify fixtures/assertions that existed only to test the removed snapshot;
- [ ] preserve caller-identity admission tests;
- [ ] preserve `allow_external` and recipient data actually required by the canonical prepared transport;
- [ ] do not create a replacement cached authority list.

Authority invariant:

~~~text
current recipient authority
= current SavedConnectionStore read
+ admit_saved_connection(person, device)
+ exact recipient check
~~~

Never:

~~~text
provider construction-time recipient snapshot
→ later Access release decision
~~~

Static gate:

~~~sh
rg -n "consented_external_recipients" crates --glob '*.rs'
~~~

Expected: no code references.

Focused validation:

~~~sh
cargo test -p floe-provider-adapters
cargo test -p floe-access
cargo test -p floe-app
~~~

## D4 — Remove dead single-attempt Conversation compatibility

Primary files:

- `crates/modules/conversation/src/turn/model_recovery.rs`;
- `crates/modules/conversation/src/turn/mod.rs`;
- `crates/modules/conversation/src/adapters/model_transport.rs`.

Changes:

- [ ] delete `generate_once`;
- [ ] delete `validate_generate_once_request`;
- [ ] delete `dispatch_generate_once`;
- [ ] remove their re-exports;
- [ ] update stale comments that still describe the removed General Conversation bridge;
- [ ] delete `TransportModelRunner::transport()` only if its caller count remains zero;
- [ ] keep `generate_with_recovery`;
- [ ] keep `ModelRequest` / `ModelRunner` / `CapabilityHost` / `AgentRuntime`;
- [ ] keep `TransportModelRunner` and `transport_request`.

Regression requirement:

The delegated Expert/Schedule tests that exercise the legacy runtime must still compile and pass. A green root Conversation suite alone is not enough proof because the retained caller is outside the canonical root path.

Static gates:

~~~sh
rg -n "generate_once|validate_generate_once_request|dispatch_generate_once" crates --glob '*.rs'
~~~

Expected: no code references to the deleted single-attempt bridge.

Focused validation:

~~~sh
cargo test -p floe-conversation
cargo test -p floe-app
cargo test -p floe-experts
~~~

## D5 — Compiler-driven fallout cleanup only

After D1-D4:

- [ ] remove imports, private helper structs, tests and comments whose only reason to exist was a deleted D1-D4 symbol;
- [ ] remove re-exports of deleted symbols;
- [ ] do not start a fresh repository-wide compatibility hunt;
- [ ] do not rename a retained legacy type to make the diff look canonical;
- [ ] do not move public-surface-only cleanup into this checkpoint when behavior remains live.

Examples of work that stays out:

- public API visibility/re-export minimization with live backing behavior → 2-E;
- delegated Expert/Schedule canonicalization → 3-A;
- FFI/Flutter/wire cleanup → Stage 3.

## D6 — Closure validation and documentation

Required full gates:

~~~sh
cargo check --workspace --lib
cargo test --workspace --no-fail-fast
python3 tools/architecture/check_boundaries.py
git diff --check
~~~

Required static gates:

- [ ] `delegate_expert_task` code refs = 0;
- [ ] `RemoteRouteResolver` code refs = 0;
- [ ] `select_remote_route` code refs = 0;
- [ ] `RemoteModelRouteResolver` code refs = 0;
- [ ] `ResolvedRemoteConnection` code refs = 0;
- [ ] `resolve_remote_model_route` code refs = 0;
- [ ] `plan_remote_route` code refs = 0;
- [ ] `consented_external_recipients` code refs = 0;
- [ ] `generate_once` / `validate_generate_once_request` / `dispatch_generate_once` code refs = 0;
- [ ] retained `generate_with_recovery` has only intentional legacy Expert/domain/test callers;
- [ ] retained `CapabilityHost` / `AgentRuntime` production callers are only deferred Expert/domain paths, not General Conversation root;
- [ ] `LegacyModelPort`, `LegacyToolPort`, `LegacyDelegationPort` remain absent from production code.

When all gates are green:

- [ ] set this document to `complete · frozen`;
- [ ] append the compact Agent report requested below;
- [ ] check 2-D in `docs/refactoring/stage-2.md`;
- [ ] move the Stage 2 current checkpoint to 2-E;
- [ ] stop. Do not start 2-E implementation in the same change set.

Known calendar mock-socket `WouldBlock` load flakiness is not automatically a 2-D blocker. If it recurs, reproduce the failing test alone and compare against pristine baseline. Do not add sleeps or weaken assertions to hide it.

## Explicit non-goals

Do not:

- delete or canonicalize `expert_compat.rs`;
- delete or canonicalize Schedule `CalendarTurn` / `AgentRuntime`;
- remove `generate_with_recovery`, `ModelRunner`, `CapabilityHost` or `TransportModelRunner`;
- remove `RemoteTurnRoute`, `AgentRemoteRouteDto` or their FFI/Flutter conversions;
- remove `SavedServerConnection`, `RemoteRoute`, `RoutePairing`, `RemoteModelConnection` merely because they are compatibility-shaped;
- wire canonical Foundation native transport;
- change model profile/routing semantics;
- change Access admit/consume/revalidate semantics;
- change Tool or Delegation recovery/replay identity;
- perform 2-E visibility cleanup beyond re-exports that disappear because their underlying D1-D4 symbol is deleted;
- start Stage 3-A;
- reopen 2-B or 2-C.

## Acceptance

2-D is complete only when:

- [ ] every deleted symbol had zero production callers immediately before deletion;
- [ ] no renamed/equivalent compatibility wrapper replaces a deleted symbol;
- [ ] retained Stage 3-A compatibility still has its production caller and tests;
- [ ] General Conversation remains on canonical Model/Tool/Delegation owners;
- [ ] focused tests for all touched owner crates are green;
- [ ] workspace compile/test is green;
- [ ] architecture checker reports zero errors/warnings;
- [ ] `git diff --check` is clean;
- [ ] remaining cleanup is correctly assigned to 2-E or Stage 3, not silently pulled into 2-D.

## Agent report format

Report only:

1. changed files/symbols by D1-D6;
2. deleted compatibility surfaces and zero-production-caller proof;
3. retained Stage 3-A compatibility and its live callers;
4. retained Stage 3 outer compatibility;
5. static-gate results;
6. focused test results;
7. workspace check/test + architecture checker + `git diff --check`;
8. known flaky/baseline result if encountered;
9. remaining 2-D blocker, if any;
10. final document/checkpoint state.

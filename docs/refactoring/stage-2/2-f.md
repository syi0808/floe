# 2-F — Stage 2 Final Validation

**Status: pending**

## Architecture gates
- [ ] `LegacyModelPort` production callers = 0;
- [ ] `LegacyToolPort` production callers = 0;
- [ ] `LegacyDelegationPort` production callers = 0;
- [ ] production `ModelPort` = `InferenceService`;
- [ ] production `ToolPort` = `ContextToolService`;
- [ ] model projection = Conversation/Context owned;
- [ ] App model route/consent/source policy = 0;
- [ ] Inference public bearer/token/arbitrary URL = 0;
- [ ] Provider does not own Access/Context business policy.

## Behavioral gates
- [ ] invalid whole batch dispatches no side effect;
- [ ] pending validated batch resumes without model re-plan;
- [ ] cross-run continuation preserves exact pending lineage;
- [ ] exact Tool/Task replay identity;
- [ ] exact recipient and revoke-before-handoff;
- [ ] post-response authority revalidation;
- [ ] model attempt settles once with durable usage;
- [ ] finalization reserve preserved.

## Commands
~~~sh
cargo check --workspace --lib
cargo test --workspace --no-fail-fast
python3 tools/architecture/check_boundaries.py
git diff --check
~~~

Mark Stage 2 complete only when all overview and 2-F checkboxes are satisfied.

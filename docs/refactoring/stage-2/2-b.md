# 2-B — General Conversation Canonical Runtime Cutover

**Status: active**

## Goal
Move the real General Conversation production path off `LegacyModelPort` and `LegacyToolPort` onto canonical Conversation/Context/Engine/Inference/Access/Provider owners.

## 2-B.1 — Runtime contract and recovery foundation

**Final P0 execution plan:** [2-b1.md](2-b1.md)

### Complete
- [x] typed `ModelConversation` and lossless Tool/Delegation exchanges;
- [x] `AuthorizedModelProjection` / `ModelProjectionPort`;
- [x] whole-batch validation before side effects;
- [x] durable `ValidatedModelBatch` / `BatchCursor`;
- [x] stable Tool/Task/Preamble identities;
- [x] one bounded Engine correction;
- [x] durable failed-attempt usage;
- [x] exact single-journal step/intent/result/cursor binding.

### Final P0 — complete / frozen
- [x] bind parent pending batch/cursor to child leading resumed batch/cursor;
- [x] preserve parent pending state when child crashes before takeover;
- [x] reject child resume with no exact parent pending match;
- [x] regress mismatched batch/cursor to `StorageUnavailable`;
- [x] regress pre-takeover child crash without pending-work loss;
- [x] freeze 2-B.1 after focused tests and workspace/boundary checks.

Primary files: `crates/modules/conversation/src/application/coordinator.rs`, `application/recovery.rs`, Vault/Conversation continuation tests.

## 2-B.2 — Canonical Model vertical slice

**Execution plan:** [2-b2.md](2-b2.md)  
**Residual P0:** [2-b2-current-authority.md](2-b2-current-authority.md)

### Access
- [x] add model dispatch request/target/permit/fence;
- [x] bind person, projection revision, coverage, data classes, purpose, consumer and exact target;
- [x] evaluate `ProcessingRestriction` without weakening LocalOnly/ApprovedRecipient;
- [x] consume immediately before provider handoff;
- [x] post-response authority revalidation;
- [x] exact-recipient/revocation/consume/revalidation tests.

### Inference
- [x] implement `InferenceService : ModelPort`;
- [x] preserve Engine attempt ID end to end;
- [x] non-secret profile observation and explicit/Auto selection;
- [x] route and exact recipient planning;
- [x] sole model usage reserve/handoff/settle owner;
- [x] transport retry/fallback owner;
- [x] explicit missing profile never silently falls back.

### Provider
- [x] separate model profiles from source connector discovery;
- [x] remove `/v1/connectors` from model lifecycle;
- [x] keep bearer/endpoint resolution private;
- [x] remove provider-owned Access/Context policy judgment;
- [x] preserve transport safeguards.

### Residual P0
- [x] production exact-recipient authority is current at admit, consume and post-response revalidation; see [2-b2-current-authority.md](2-b2-current-authority.md).

### Exit
- [x] production `ModelPort` = `InferenceService`;
- [x] `LegacyModelPort` production calls = 0;
- [x] App Foundation/Server choice = 0;
- [x] Inference public raw bearer/token/arbitrary endpoint = 0.

## 2-B.3 — Canonical Context projection and Tool vertical slice

**Execution plan:** [2-b3.md](2-b3.md)

### Projection
- [x] Context-owned model projection assembly;
- [x] route/recipient/credential excluded from projection;
- [x] current source/grant/freshness/history reauthorization;
- [x] `TransitionalModelProjection` production calls = 0;
- [x] Inference has no concrete Context dependency.

### Tool
- [x] `ContextToolService : ToolPort`;
- [x] Context owns Manager `ToolDescriptor` definitions;
- [x] direct `ToolResult.coverage` and artifacts;
- [x] remove ResultRecorder → coverage re-query side channel;
- [x] tool/source availability independent from model route;
- [x] `LegacyToolPort` production calls = 0;
- [x] App `ConversationCapabilities` business ownership = 0.

## 2-B.4 — App production cutover

**Execution plan:** [2-b4.md](2-b4.md)

- [x] remove General Conversation pre-turn `HostInferenceRoutes.resolve()`;
- [x] remove model-execution use of `ConversationTurnRequest.remote_route`;
- [x] remove App `Model` / `RootModel` / `GovernedModel` policy path;
- [x] remove App model consent/routing decisions;
- [x] wire canonical projection/model/tools with temporary delegation bridge;
- [x] delete legacy General Conversation conversion helpers after caller count reaches 0.

Primary files: `crates/app/src/composition.rs`, `inference_routes.rs`, `turn_request.rs`, `vault_host/conversation_turn.rs`, `engine_ports.rs`.

## 2-B.5 — Integrated hardening

- [ ] exact recipient and revoke-before-handoff;
- [ ] revoke-during-call/post-response suppression;
- [ ] history/source coverage and freshness reauthorization;
- [ ] no raw secret outside provider/credential boundary;
- [ ] validated-batch recovery including cross-run lineage;
- [ ] stable Tool/Task replay identity;
- [ ] one attempt ID and one model usage owner;
- [ ] finalization reserve not double charged;
- [ ] App no longer owns model/source/access policy.

## Validation
~~~sh
cargo check --workspace --lib
cargo test --workspace --no-fail-fast
python3 tools/architecture/check_boundaries.py
git diff --check
~~~

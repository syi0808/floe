# 2-C — Delegation Ownership Convergence

**Status: pending**

## Goal
Remove `LegacyDelegationPort` and hidden staged endpoint context so delegation is an explicit Experts-owned Task/endpoint contract.

## Checklist
- [ ] inventory `LegacyDelegationPort` callers and staged endpoint maps;
- [ ] define minimum explicit endpoint context for Schedule and built-in Experts;
- [ ] keep Task lifecycle, Expert identity/revision and cancellation under Experts;
- [ ] pass context through admitted Task/endpoint contracts, not App mutable maps;
- [ ] remove App `stage()/clear()` ownership;
- [ ] preserve stable TaskId/replay identity across crashes;
- [ ] preserve denied/timeout/cancelled/rejected typed Task outcomes;
- [ ] remove `LegacyDelegationPort` after final production caller moves.

Primary files: `crates/app/src/vault_host/conversation_turn/engine_ports.rs`, `expert_dispatch.rs`, `expert_dispatch/schedule.rs`, `crates/modules/experts/src/*`.

## Acceptance
- [ ] no run-id → endpoint-context mutable authority in App;
- [ ] endpoint input explicit and bounded;
- [ ] built-in/Schedule use one Task lifecycle;
- [ ] `LegacyDelegationPort` production references = 0.

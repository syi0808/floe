# 2-E — Internal Public-Surface Closure

**Status: complete · frozen**

**Code baseline reviewed:** a8b21b6651f2921ac53142394658d4d3d96c6596

This document is the authoritative execution plan for 2-E.

2-D is complete/frozen. Do not reopen 2-B through 2-D, and do not pull Stage 3-A Expert/domain convergence or Stage 3 FFI/product-boundary deletion into this slice.

## Goal

Make the Stage 2 crate roots express real owner APIs instead of exposing:

- internal module topology;
- migration-only re-exports of another owner's types;
- App-only composition helpers;
- test/convenience paths that external production code does not need.

2-E is a **visibility and ownership-surface closure**, not a behavior cutover.

At exit:

~~~text
external Rust caller
  → owner crate root API
  → owned contract/service/port

not

external Rust caller
  → owner::application/...
  → owner::ports/...
  → owner::turn/...
  → App composition helper
  → migration alias of another owner's type
~~~

The General Conversation runtime, delegated Expert/Schedule compatibility, FFI wire behavior, route semantics, authority checks, recovery identity and provider transport behavior must remain semantically unchanged.

## Fixed classification rule

Before changing visibility, classify each symbol by its current caller.

| Surface | 2-E action |
| --- | --- |
| canonical owner API used cross-crate | keep public at the owner crate root |
| internal module path when the same value is already available from the crate root | make module private and move callers to the root |
| another owner's type re-exported as migration convenience | move callers to the real owner and remove the alias |
| App-only composition/helper with no external product caller | remove root export and reduce to crate-private |
| delegated Expert/Schedule behavior still used in production | keep until Stage 3-A |
| FFI/Flutter/native/server or remote-authority outer caller still exists | keep until Stage 3 |
| secret-bearing legacy compatibility type with an exact deferred caller | keep only for that caller; do not relocate it for grep cleanliness |

A name containing `legacy`, `compat`, `internal`, `route` or `transport` is not sufficient evidence for reducing visibility.

## Revalidated public-surface inventory

### Conversation

Current crate root exposes both the owner API and internal topology:

~~~rust
pub mod adapters;
pub mod prompts;
pub mod turn;
...
pub use turn::*;
~~~

The reviewed call graph shows:

- no external production caller needs `floe_conversation::adapters::...`;
- the only direct `floe_conversation::turn::...` caller is App test/runtime compatibility that can use the crate root or the actual owner;
- `floe_conversation::prompts::manager_prompt` is still used by App/Stage 3-A and remains a legitimate public module;
- `turn::*` currently re-exports several values actually owned by `floe-agent-contract`, `floe-inference` or `floe-execution`.

Known migration aliases to remove from the Conversation public surface include:

- `AGENT_VERSION` → `floe_agent_contract::AGENT_VERSION` / kernel owner;
- `CapabilityDescriptor`, `CapabilityExecution`, `CapabilityExecutionState`,
  `ProviderReplay`, `ModelReplay`, envelope/manifest values → `floe-agent-contract`;
- `ModelStep` → `floe-inference`;
- `ModelUsage`, `UsageAttempt` → `floe-execution`;
- `UsageLedger` → `floe-inference`.

Do not remove the following live Stage 3-A compatibility simply because it lives under `turn` today:

- `AgentRuntime`;
- `AgentBudget`;
- `AgentCommand`;
- `AgentSession`, `AgentSessionScope`, `AgentMessage`, `AgentOutcome`,
  `AgentEvent`, `AgentEventKind`, `AgentUsage`, continuation/recovery session values;
- `SessionStore`;
- `CapabilityHost`, `CapabilityInvocation`;
- `ModelRequest`, `ModelResponse`, `ModelRunner`;
- `generate_with_recovery`;
- `TransportModelRunner`, `transport_request`;
- `bounded_source_history_start`, `carries_source_history`.

Those remain public only because delegated Expert/Schedule/domain callers still cross the crate boundary. Their semantic removal belongs to Stage 3-A.

### Inference

Current root exposes canonical contracts plus compatibility and also exposes `pub mod ports`.

The reviewed call graph shows:

- canonical provider/transport types are already re-exported from `floe_inference::*`;
- the remaining direct `floe_inference::ports::...` caller is a provider-adapter test and can import the root API;
- `LEGACY_INFERENCE_CONSUMER` and `MODEL_GENERATION_CAPABILITY` are internal route-config details and have no cross-crate caller;
- `EVERYDAY_ASSISTANCE_PURPOSE`, `InferenceRouter`, `RoutePlanError`,
  attempt values, model-provider/transport contracts and other current cross-crate owner values remain public.

The following secret-bearing compatibility values are **not** automatically deleted or relocated in 2-E:

- `SavedServerConnection`;
- `RemoteModelConnection`;
- `RemoteRoute`;
- `RoutePairing`;
- `ModelRouteConfig`;
- `candidate_route`.

They still serve delegated Expert/domain or outer remote-authority/pairing callers. Stage 2's secret-boundary invariant applies to the **canonical model-dispatch public contract**: `InferenceService`, model profiles, canonical requests and prepared transports must expose no bearer/base URL. Do not distort live Stage 3 compatibility merely to make a repository-wide grep for `bearer` empty.

### Context

Current root has complete owner re-exports but also exposes:

~~~rust
pub mod application { ... }
pub mod ports { ... }
~~~

External direct application-path callers are limited to the Day context-view helpers:

- `task_context_view`;
- `note_context_view`.

Promote those two functions to the Context crate root, update callers, then hide the `application` and `ports` module topology. All other existing root Context contracts remain available.

### Access

Current root already re-exports the authority/admission/grant/fence contracts required cross-crate, while also exposing:

~~~rust
pub mod application;
pub mod ports;
~~~

No reviewed external caller requires those nested module paths. Hide both module trees and preserve the root public API.

### App

The App crate is a product/composition boundary, so 2-E must distinguish internal composition from Stage 3 outer callers.

Safe internal closures on the reviewed baseline:

- `pub mod local_context` → private module; keep required root types;
- `pub mod turn_request` → private module; keep `ConversationTurnRequest` and `RemoteTurnRoute` root exports;
- `pub mod worker` → private module; keep the existing worker command/result root exports;
- remove the root export of `HostInferenceRoutes` and reduce the type itself to crate-private;
- remove the App root re-export of `PublishedCalendarObservation`; App already consumes the Context owner value directly;
- remove the App root export of `local_identity_for_database` and make/use it as an internal bootstrap helper if current caller revalidation still shows only App composition.

Do **not** close these yet:

- `pub mod agent_run`: FFI currently calls `floe_app::agent_run::run`;
- `pub mod events`: FFI currently imports `events::{EventPayload, EventRead, RunEventRecord}`;
- `RemoteTurnRoute`, `RemoteRoute`, `RoutePairing`, `AgentOutcome` and current worker/root product values used by FFI;
- AppHost/FFI compatibility constructors and `legacy_services()`;
- protocol/Flutter/native/server DTOs.

Those are Stage 3 product-boundary callers.

## Execution sequence

## E0 — Freeze and revalidate the boundary

Read only the documents relevant to this slice:

1. `AGENTS.md`;
2. `docs/refactoring/stage-2.md`;
3. this file;
4. `docs/refactoring/stage-3/3-a.md`;
5. `docs/refactoring/stage-3/3-c.md`;
6. `docs/refactoring/stage-3/3-e.md`.

Before code edits:

- [x] confirm HEAD still descends from 2-D closure `a8b21b6651f2921ac53142394658d4d3d96c6596`;
- [x] confirm 2-D remains complete/frozen;
- [x] re-run caller inventory for every visibility change below;
- [x] classify any newly discovered caller as internal, Stage 3-A, or Stage 3 outer before changing it;
- [x] do not perform a new dead-code audit or behavioral refactor.

A compile error caused by the intended visibility closure is a caller inventory signal. Move that caller to the canonical owner/root path; do not reopen the module or add a replacement compatibility re-export unless the caller is genuinely part of the intended public API.

## E1 — Close Conversation topology and migration aliases

Primary files:

- `crates/modules/conversation/src/lib.rs`;
- `crates/modules/conversation/src/turn/mod.rs`;
- `crates/modules/conversation/src/turn/session.rs`;
- `crates/modules/conversation/src/turn/usage.rs`;
- direct callers exposed by compiler/search, especially App, Vault adapter and Conversation tests.

Changes:

- [x] change `pub mod adapters` to an internal module;
- [x] change `pub mod turn` to an internal module;
- [x] keep `pub mod prompts`;
- [x] replace `pub use turn::*` with an explicit list of Conversation-owned values that have real cross-crate callers;
- [x] keep the live Stage 3-A legacy runtime surface listed in the inventory above;
- [x] stop re-exporting another owner's contract/transport/accounting values from `turn/session.rs` and `turn/usage.rs`;
- [x] migrate callers of those aliases to `floe-agent-contract`, `floe-inference` or `floe-execution`;
- [x] remove the duplicate public Conversation `AGENT_VERSION` alias/constant in favor of the shared contract version;
- [x] keep internal-only helpers such as `context_manifest`, `project_source_history`,
  `sync_usage` and `turn_ledger` reachable only inside Conversation unless revalidation finds a legitimate cross-crate owner caller;
- [x] update the one direct `floe_conversation::turn::UsageLedger` use to the root/owner path;
- [x] do not change legacy model retry/accounting behavior.

Known caller migrations include, but are not limited to:

~~~text
floe_conversation::CapabilityDescriptor
  → floe_agent_contract::CapabilityDescriptor

floe_conversation::CapabilityExecution*
  → floe_agent_contract::CapabilityExecution*

floe_conversation::ProviderReplay / ModelReplay
  → floe_agent_contract

floe_conversation::ModelStep
  → floe_inference::ModelStep

floe_conversation::ModelUsage
  → floe_execution::budget::ModelUsage

floe_conversation::UsageLedger
  → floe_inference::UsageLedger

floe_conversation::AGENT_VERSION
  → floe_agent_contract::AGENT_VERSION
~~~

Do not migrate Conversation-owned Session/Run/runtime types to another crate merely to shrink the list.

Static checks after E1:

~~~sh
rg -n "floe_conversation::(adapters|turn)::" crates --glob '*.rs'
rg -n "floe_conversation::(AGENT_VERSION|CapabilityDescriptor|CapabilityExecution|CapabilityExecutionState|ProviderReplay|ModelReplay|ModelStep|ModelUsage|UsageAttempt|UsageLedger)" crates --glob '*.rs'
~~~

Expected: no cross-crate use of the closed nested paths or wrong-owner aliases.

Focused validation:

~~~sh
cargo check -p floe-conversation
cargo test -p floe-conversation
cargo test -p floe-vault
cargo test -p floe-app
~~~

## E2 — Close Inference module topology without moving live compatibility

Primary files:

- `crates/modules/inference/src/lib.rs`;
- `crates/modules/inference/src/application/mod.rs`;
- `crates/modules/inference/src/application/route_config.rs`;
- `crates/modules/inference/src/ports/mod.rs`;
- direct provider/test callers.

Changes:

- [x] change `pub mod ports` to an internal module;
- [x] move any direct `floe_inference::ports::...` caller to the existing root re-export;
- [x] remove `LEGACY_INFERENCE_CONSUMER` and `MODEL_GENERATION_CAPABILITY` from the crate root public exports;
- [x] reduce those constants to internal visibility;
- [x] preserve public canonical provider/transport/service/profile/attempt APIs with real cross-crate callers;
- [x] preserve the exact legacy/outer route values still needed by Stage 3-A/Stage 3;
- [x] preserve `AttemptLifecycle` / `AttemptUpdate` while the legacy Conversation runtime consumes them;
- [x] do not move bearer/base URL into a new handle or wrapper just to change type names.

Canonical secret-boundary proof must continue to cover:

~~~text
AuthorizedModelProjection
→ InferenceService
→ ModelProfile / PreparedModelProfile
→ CanonicalModelRequest
→ PreparedModelTransport
~~~

None of those canonical handoff types may gain bearer/token/arbitrary URL fields.

Static checks after E2:

~~~sh
rg -n "floe_inference::ports::" crates --glob '*.rs'
rg -n "floe_inference::(LEGACY_INFERENCE_CONSUMER|MODEL_GENERATION_CAPABILITY)" crates --glob '*.rs'
~~~

Expected: no cross-crate nested-port access or external use of the internal constants.

Focused validation:

~~~sh
cargo check -p floe-inference
cargo test -p floe-inference
cargo test -p floe-provider-adapters
cargo test -p floe-knowledge
cargo test -p floe-conversation
~~~

## E3 — Close Context and Access module topology

### Context

Primary files:

- `crates/modules/context/src/lib.rs`;
- `crates/modules/context/src/application/day_context_views.rs`;
- App callers in `vault_host/conversation_turn.rs` and `expert_compat.rs`;
- Context integration tests.

Changes:

- [x] root re-export `task_context_view` and `note_context_view`;
- [x] move all external callers from
  `floe_context::application::day_context_views::...` to `floe_context::...`;
- [x] make the top-level `application` module private;
- [x] make the top-level `ports` module private;
- [x] preserve all current root projection/source/tool/observation contracts;
- [x] do not change acquisition/provenance/freshness behavior.

### Access

Primary files:

- `crates/modules/access/src/lib.rs`;
- `crates/modules/access/src/application/mod.rs`;
- `crates/modules/access/src/ports/mod.rs`.

Changes:

- [x] make top-level `application` private;
- [x] make top-level `ports` private;
- [x] preserve all root authority/admission/grant/release/model-dispatch contracts with live callers;
- [x] do not change any admit/consume/revalidate or grant semantics.

Static checks after E3:

~~~sh
rg -n "floe_context::(application|ports)::" crates --glob '*.rs'
rg -n "floe_access::(application|ports)::" crates --glob '*.rs'
~~~

Expected: no external nested-module caller.

Focused validation:

~~~sh
cargo check -p floe-context
cargo test -p floe-context
cargo check -p floe-access
cargo test -p floe-access
cargo test -p floe-app
~~~

## E4 — Close App-only composition surface and preserve Stage 3 outer callers

Primary files:

- `crates/app/src/lib.rs`;
- `crates/app/src/inference_routes.rs`;
- `crates/app/src/bootstrap.rs`;
- `crates/app/src/composition.rs`;
- module declarations for `local_context`, `turn_request`, `worker`;
- FFI compile callers as regression surface only.

Changes:

- [x] make `local_context` an internal module while preserving its required root exports;
- [x] make `turn_request` an internal module while preserving
  `ConversationTurnRequest` and `RemoteTurnRoute` at the App root;
- [x] make `worker` an internal module while preserving the existing public worker command/result root exports;
- [x] keep `agent_run` public because current FFI calls `floe_app::agent_run::run`;
- [x] keep `events` public because current FFI imports event types through that module;
- [x] remove `HostInferenceRoutes` from the App root and reduce it to crate-private;
- [x] remove App's migration-only `PublishedCalendarObservation` root re-export;
- [x] remove the App root export of `local_identity_for_database` and reduce/use the helper internally if revalidation still shows no external product caller;
- [x] preserve all FFI-used root types, including current owner-value re-exports needed by wire conversion;
- [x] preserve `RemoteRoute`, `RoutePairing`, `RemoteTurnRoute`, remote authority/pairing worker operations and `AgentOutcome`;
- [x] preserve `AppHost::legacy_services()` and other explicit outer compatibility for Stage 3;
- [x] do not edit protocol DTOs, Flutter models or native/server callers.

Static checks after E4:

~~~sh
rg -n "floe_app::(local_context|turn_request|worker)::" crates --glob '*.rs'
rg -n "floe_app::HostInferenceRoutes|floe_app::PublishedCalendarObservation|floe_app::local_identity_for_database" crates --glob '*.rs'
~~~

Expected: no external caller to the newly closed App internals.

Focused validation:

~~~sh
cargo check -p floe-app
cargo test -p floe-app
cargo check -p floe-ffi
cargo test -p floe-ffi
~~~

The FFI gate is mandatory here because E4 changes the Rust surface immediately behind the product boundary even though Stage 3 wire semantics are intentionally untouched.

## E5 — External compile probes and public-surface proof

Use one temporary external Rust crate outside the repository source tree. Depend on the relevant Floe crates by local path. Do not commit the probe.

### Positive probes

The external crate must still compile using owner/root APIs needed after 2-E, including representative values from:

- Conversation canonical API: `ConversationService`, `ConversationPorts`, `TurnRequest`,
  Session/Run values, plus the explicitly retained Stage 3-A compatibility;
- Inference root: `InferenceService`, model profile/provider/transport contracts;
- Context root: projection/tool/source APIs plus `task_context_view` / `note_context_view`;
- Access root: model-dispatch and authority/grant/release contracts;
- App root: `ConversationTurnRequest`, `RemoteTurnRoute`, current worker command/result values;
- App Stage 3 exceptions: `agent_run::run` and `events::EventRead` remain accessible.

### Negative probes

Separate compile-fail probes must prove an external crate cannot use:

- `floe_conversation::adapters::...`;
- `floe_conversation::turn::...`;
- removed Conversation wrong-owner aliases such as `floe_conversation::ModelStep` and
  `floe_conversation::UsageLedger`;
- `floe_inference::ports::...`;
- `floe_inference::LEGACY_INFERENCE_CONSUMER`;
- `floe_inference::MODEL_GENERATION_CAPABILITY`;
- `floe_context::application::...`;
- `floe_context::ports::...`;
- `floe_access::application::...`;
- `floe_access::ports::...`;
- `floe_app::local_context::...`;
- `floe_app::turn_request::...`;
- `floe_app::worker::...`;
- `floe_app::HostInferenceRoutes`;
- `floe_app::PublishedCalendarObservation`;
- `floe_app::local_identity_for_database`.

A compile-fail probe must fail because of visibility/missing export, not because the sample contains an unrelated type error.

### Secret-boundary proof

Also inspect/compile representative canonical types and verify:

- `ConversationTurnRequest` cannot read/inject a saved credential;
- `AuthorizedModelProjection`, canonical model dispatch and provider-profile handoff expose no raw bearer/token/base URL;
- no new public getter/debug/serialization path was added to expose provider credentials.

Do not require legacy/outer `RemoteRoute` or `SavedServerConnection` to disappear in this stage; prove instead that only the explicitly deferred callers retain them.

## E6 — Closure validation and documentation

Required full gates:

~~~sh
cargo check --workspace --lib
cargo test --workspace --no-fail-fast
python3 tools/architecture/check_boundaries.py
git diff --check
~~~

Required static/public-surface gates:

- [x] Conversation `adapters` and `turn` are not externally reachable;
- [x] Conversation wrong-owner migration aliases listed in E1 are gone;
- [x] `prompts` and all live Stage 3-A Conversation contracts remain usable;
- [x] Inference `ports` is not externally reachable;
- [x] Inference internal route constants are not public;
- [x] canonical Inference handoff remains secret-free;
- [x] Context `application` / `ports` are not externally reachable;
- [x] Access `application` / `ports` are not externally reachable;
- [x] Context root exposes the two Day context-view helpers required by current callers;
- [x] App `local_context` / `turn_request` / `worker` module paths are not externally reachable;
- [x] App-only `HostInferenceRoutes` and bootstrap helper are not public;
- [x] FFI-required `agent_run`, `events`, root worker/turn values and remote compatibility still compile;
- [x] no Stage 3-A runtime behavior was removed or canonicalized;
- [x] no Stage 3 DTO/ABI field was changed.

When every gate is green:

- [x] set this document to `complete · frozen`;
- [x] append the compact Agent report requested below;
- [x] check 2-E in `docs/refactoring/stage-2.md`;
- [x] move the Stage 2 current checkpoint to 2-F;
- [x] stop. Do not start 2-F implementation in the same change set.

The known calendar mock-socket `WouldBlock` load flake follows the same rule as 2-D: reproduce alone and compare to pristine baseline; do not add sleeps or weaken assertions.

## Explicit non-goals

Do not:

- remove or canonicalize `generate_with_recovery`, `AgentRuntime`, `ModelRunner`,
  `CapabilityHost`, `TransportModelRunner` or Schedule `CalendarTurn`;
- edit `expert_compat.rs` except for import-path/owner-alias migration required by E1/E3;
- relocate/delete `SavedServerConnection`, `RemoteRoute`, `RemoteModelConnection`,
  `RoutePairing`, `ModelRouteConfig` or `candidate_route` while deferred callers remain;
- delete `agent_run` or `events` App module paths used by FFI;
- delete `RemoteTurnRoute`, `AgentRemoteRouteDto` or remote-route conversion;
- change AppHost/FFI/Flutter/native/server product contracts;
- wire canonical Foundation native transport;
- change model routing/profile/fallback behavior;
- change Access authority/grant/fence semantics;
- change Context acquisition/provenance/freshness semantics;
- change Tool/Delegation replay identity or Conversation recovery;
- perform Stage 3-A, 3-B, 3-C or 3-E work;
- reopen 2-B, 2-C or 2-D.

## Acceptance

2-E is complete only when:

- [x] every visibility reduction is backed by current caller inventory;
- [x] owner crate roots remain sufficient for legitimate cross-crate use;
- [x] wrong-owner migration aliases are removed instead of renamed;
- [x] internal module topology is hidden where a stable root API already exists;
- [x] live Stage 3-A and Stage 3 compatibility remains intact;
- [x] canonical model/projection/access boundaries remain secret-free and owner-correct;
- [x] positive and negative external compile probes behave as specified;
- [x] focused tests are green;
- [x] workspace compile/test is green;
- [x] architecture checker reports zero errors/warnings;
- [x] `git diff --check` is clean;
- [x] the next and only checkpoint is 2-F.

## Agent report format

Report only:

1. changed files/symbols by E1-E6;
2. final Conversation public surface and owner-alias migrations;
3. final Inference public surface, including canonical secret-free proof and deferred compatibility;
4. Context/Access module-topology closure and root API retained;
5. App internal-surface closure and Stage 3 outer surface retained;
6. external compile-probe positive/negative results;
7. focused test results;
8. workspace check/test + architecture checker + `git diff --check`;
9. known flaky/baseline result if encountered;
10. remaining 2-E blocker, if any;
11. final document/checkpoint state.

## Agent report (E6 closure)

1. Changed files/symbols by E1-E6 (29 code files, visibility/imports only; no
   behavior, retry/accounting, routing, authority, DTO or ABI change):
   - E0: HEAD `95d89d5` descends from 2-D closure `a8b21b6`; tree clean;
     2-D `complete · frozen` untouched; caller inventory re-run per plan.
     Newly confirmed callers all fit the plan: 9×
     `floe_conversation::turn::UsageLedger` (App tests),
     `floe_inference::ports::…` (one provider-adapter test),
     `floe_context::application::day_context_views::task_context_view`
     (App ×2 plus one Context integration test).
   - E1: `conversation/src/lib.rs` (`mod adapters`, `mod turn`,
     explicit root list replacing `pub use turn::*`);
     `turn/session.rs` (owner re-exports → private `use`,
     duplicate `AGENT_VERSION` deleted in favor of
     `floe_agent_contract::AGENT_VERSION`, `UsageLedger` fields →
     `floe_inference::UsageLedger`); `turn/usage.rs` (`pub use` →
     `use`); `turn/mod.rs` (usage re-export narrowed to
     `sync_usage`/`turn_ledger`); `turn/runtime.rs`,
     `turn/model_recovery.rs` (owner imports); caller migration in
     App (6 files), Vault (3 files), `turn_runtime.rs`.
   - E2: `inference/src/lib.rs` (`mod ports`, two internal constants
     off the root); `application/mod.rs` (same); `route_config.rs`
     (constants → `pub(crate)`); provider-adapter test → root import.
   - E3: `context/src/lib.rs` (`mod application`, `mod ports`, root
     `task_context_view`/`note_context_view`); `access/src/lib.rs`
     (`mod application`, `mod ports`); App ×2 + `native_context.rs`
     → root paths; trimmed two newly unused internal re-exports
     (`access/application/mod.rs`, `context personal_sources.rs`).
   - E4: `app/src/lib.rs` (`mod local_context`/`turn_request`/`worker`,
     dropped `HostInferenceRoutes`, `PublishedCalendarObservation`,
     `local_identity_for_database` root exports);
     `inference_routes.rs`/`bootstrap.rs` → `pub(crate)`;
     `composition.rs` → `crate::bootstrap::…` path.
   - E5: external probes under `/tmp/e5` (not committed).
   - E6: this document + `stage-2.md` checkpoint move.
2. Final Conversation public surface: canonical root (`api`/`application`/
   `domain`/`ports`, `Archive*`, `FinalPayloadValidator`) unchanged;
   `pub mod prompts` kept; explicit turn list =
   `AgentBudget/Command/Continuation/Event/EventKind/Message/Outcome/`
   `Runtime/Session/SessionScope/Usage`, `CapabilityHost/Invocation`,
   `DelegationExecution/State` (required by `AgentSession`'s public
   interface), `ModelRequest/Response/Runner`, `SessionRecoveryPointer`,
   `SessionStore`, `TransportModelRunner`, `transport_request`,
   `generate_with_recovery`, `bounded_source_history_start`,
   `carries_source_history`. Owner-alias migrations:
   `AGENT_VERSION`→`floe_agent_contract`, `CapabilityDescriptor`/
   `CapabilityExecution*`/`ProviderReplay`/`ModelReplay`→contract,
   `ModelStep`→`floe_inference`, `ModelUsage`→`floe_execution::budget`,
   `UsageLedger`→`floe_inference` (incl. all 9 `turn::UsageLedger`
   uses); `context_manifest`/`project_source_history`/`sync_usage`/
   `turn_ledger` now crate-internal. No legacy retry/accounting change.
3. Final Inference public surface: canonical contracts unchanged
   (`InferenceService`, profiles, provider/transport contracts,
   `InferenceRouter`, `RoutePlanError`, attempts, `UsageLedger`,
   `AttemptLifecycle`/`AttemptUpdate`, `EVERYDAY_ASSISTANCE_PURPOSE`);
   `ports` hidden; `LEGACY_INFERENCE_CONSUMER`/
   `MODEL_GENERATION_CAPABILITY` now `pub(crate)`.
   Secret-free proof: `AuthorizedModelProjection`,
   `CanonicalModelRequest/Response`, `PreparedModelProfile/Transport`,
   `ModelProfile`, `ModelDispatchRequest/Target` carry no
   bearer/token/base-URL fields (only doc comments asserting absence
   plus token-budget counters); the 2-E diff adds no new
   `pub fn/struct/enum/const/type/trait`. Deferred compatibility kept
   untouched: `SavedServerConnection`, `RemoteModelConnection`,
   `RemoteRoute`, `RoutePairing`, `ModelRouteConfig`, `candidate_route`
   — retained only by the credential owner (provider adapters), the
   canonical App admission path, Stage 3-A legacy endpoint stores, and
   Stage 3 outer/FFI callers (enumerated, none moved).
4. Context/Access closure: both `application` and `ports` top-level
   modules are now private in each crate; every other root contract
   retained. Context root newly exposes `task_context_view` and
   `note_context_view` (Day helpers, no signature change); App ×2 and
   `native_context.rs` migrated to the root. No
   acquisition/provenance/freshness or admit/consume/revalidate/grant
   semantic change.
5. App closure: `local_context`/`turn_request`/`worker` now internal
   with all existing root command/result/type exports preserved
   (`ConversationTurnRequest`, `RemoteTurnRoute`, worker operations/
   results, local-context host/outcome values);
   `HostInferenceRoutes` and `local_identity_for_database` reduced to
   `pub(crate)` (sole callers in-crate);
   `PublishedCalendarObservation` App alias removed (owner
   `floe_context` used directly). Stage 3 outer surface fully
   retained and compiling: `pub mod agent_run` (`run`),
   `pub mod events` (`EventPayload`/`EventRead`/`RunEventRecord`),
   `RemoteTurnRoute`/`RemoteRoute`/`RoutePairing`/`AgentOutcome`,
   `AppHost::legacy_services()`, worker/root product values, all
   protocol/Flutter/native/server DTOs (bindings untouched).
6. External compile probes (temporary crates in `/tmp/e5`, path deps,
   shared target dir, never committed): positive probe covering every
   E5 bullet (Conversation canonical + Stage 3-A compat, Inference
   root + deferred compat, Context root + Day helpers, Access
   dispatch/authority/grant contracts, App root + `agent_run::run` +
   `events::EventRead`) compiles clean. 21/21 negative probes reject
   with the exact visibility reason: 10× E0603 (private `adapters`,
   `turn`, `ports` ×2, `application` ×2, `local_context`,
   `turn_request`, `worker`) and 11× E0432 (removed
   `ModelStep`/`UsageLedger`/`ModelUsage`/`CapabilityDescriptor`/
   `AGENT_VERSION`/`ProviderReplay`,
   `LEGACY_INFERENCE_CONSUMER`/`MODEL_GENERATION_CAPABILITY`,
   `HostInferenceRoutes`/`PublishedCalendarObservation`/
   `local_identity_for_database`), each message spot-checked
   (`private module` / `no … in the root`).
7. Focused results, all green: E1 conversation lib 94 +
   `turn_runtime` 46, vault lib 79 + `agent_vault` 21, app lib 194 +
   2 + 5; E2 inference lib 21 + `usage_settlement` 3,
   provider-adapters lib 63 + `native_calendar` 1, knowledge lib 29,
   conversation 94 + 46; E3 context lib 62 + 20 + 3 + 6 + 9, access
   lib 29, app 194 + 2 + 5; E4 app 194 + 2 + 5, ffi lib 11 + `c_abi`
   13 + `native_action` 1.
8. Workspace gates: `cargo check --workspace --lib` clean;
   `cargo test --workspace --no-fail-fast` exit 0, 994 passed /
   0 failed across 94 ok suites;
   `tools/architecture/check_boundaries.py` errors [] warnings [];
   `git diff --check` clean. New warnings limited to 4, all inherent
   to the mandated closure and gate-neutral: `project_source_history`
   (+`project_messages`, unused re-export) kept per plan with zero
   callers, and `WorkerTrace` kept per the no-new-dead-code-audit
   rule (zero-caller alias previously reachable only via the closed
   `worker::` path); baseline warnings verified identical via a
   pristine worktree comparison.
9. Known flaky/baseline result: none encountered. The plan's calendar
   mock-socket `WouldBlock` flake did not trigger. Note:
   `agent_vault`'s log line `1 passed / 20 filtered` is the
   `vault_lock_child` test's own child-process re-execution by
   design, not a skip — the binary passes 21/21 directly and in
   every cargo run, with 0 failures.
10. Remaining 2-E blocker: none.
11. Final state: this document `complete · frozen` (77/77 boxes);
    `docs/refactoring/stage-2.md` 2-E checked; Stage 2 current
    checkpoint moved to 2-F. 2-F implementation not started.

# 3-A — Remaining Root and Domain Caller Convergence

**Status: pending**

## Goal
Move root/domain callers intentionally left outside the Stage 2 General Conversation cutover onto the same canonical owner services.

## Checklist
- [ ] inventory model entry points outside canonical `InferenceService`;
- [ ] classify each as Conversation, Expert or domain-owned caller;
- [ ] canonicalize `run_calendar_agent_turn`;
- [ ] canonicalize or remove `CalendarTurn` compatibility;
- [ ] route Schedule/Calendar through canonical Context/Access/Inference;
- [ ] remove duplicate model accounting/retry from domain roots;
- [ ] preserve Expert Task identity, coverage and provenance;
- [ ] remove old Calendar-specific model route path.

Primary file: `crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule/agent.rs`.

## Acceptance
- [ ] remaining domain roots do not reconstruct provider route/credential policy;
- [ ] no duplicate domain-specific model usage owner.

# 3-A — Remaining Root and Domain Caller Convergence

**Status: residual closure ready**

**Planning baseline:** `170770d314bd0b6bef0a6370cc367222fe9399bb`  
**Code baseline:** `39263a1339d0519b78a20680c0eea73506ff6d6f` (`2-F`; the planning commit after it changed docs only)  
**Residual-review baseline:** `8bdd50628819119331c8c1b67307eaee8392bab9` (`3-A` first-pass closure)

This document is the authoritative execution plan for 3-A.


## Residual closure authority

The first-pass 3-A implementation at \`8bdd50628819119331c8c1b67307eaee8392bab9\` completed the intended A0/A1/A3 convergence and canonicalized the **production Schedule endpoint**. A post-closure review found one narrow inconsistency with this plan's own A2/A4 deletion rule:

~~~text
run_calendar_agent_turn
CalendarAgentTurnRequest / CalendarAgentTurnResult / CalendarAgentProposal
CalendarTurn
CalendarModel
schedule/legacy_model.rs :: LegacyScheduleModelHost
~~~

remain in production source, but the reviewed baseline has no live product caller for that path. The remaining callers are test/fixture code in:

- \`crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule/agent/tests.rs\`;
- \`crates/app/src/vault_host/tests/proposals.rs\`.

The compatibility path therefore does **not** qualify for the 3-D/3-E live-caller deferral. Keeping a production parallel model runtime solely to support tests violates the fixed 3-A design:

> caller-zero direct fallout is deleted in 3-A; tests do not justify a production compatibility path.

This is a **narrow residual closure**, not a broad reopen. A0, A1 and A3 are frozen as implemented. Do not redesign shared Inference, Foundation transport, Expert contracts, Knowledge Learner, TaskCoordinator, Context projection or Access.

Where this residual section conflicts with the first-pass A2/A4 report's decision to defer the Schedule agent turn, **R0-R4 below are authoritative**.

### Residual goal

Delete the caller-zero Schedule agent-turn runtime, move or retire the tests that still depend on it without losing owner-level regressions, re-prove 3-A gates, and then freeze 3-A for real.

Expected production-code direction:

~~~text
ScheduleEndpoint
  → run_calendar_expert_endpoint
  → ExpertModelHost
  → InferenceExecutor
  → Context / Access / Provider

NO second path:
  run_calendar_agent_turn
  → CalendarTurn / CalendarModel
  → LegacyScheduleModelHost
  → TransportModelRunner / generate_with_recovery
~~~

### Explicit residual non-goals

Do not:

- redo A0 canonical Foundation work;
- change \`InferenceExecutor\` / \`InferenceExecutionConstraint\` unless deletion exposes a direct compile-only fallout;
- change Expert model-requirement semantics;
- change Knowledge Learner;
- delete the separate live AgentFixture runtime — it remains 3-D/3-E product-boundary debt;
- start AppHost 3-B work;
- start protocol/FFI/Flutter work;
- weaken or delete an owner invariant merely because its old test used \`run_calendar_agent_turn\`;
- introduce another production compatibility wrapper or test-only public production API.

---

## R0 — Prove the residual caller set

Run against the execution HEAD before editing:

~~~sh
rg -n \
  'run_calendar_agent_turn|CalendarAgentTurnRequest|CalendarAgentTurnResult|CalendarAgentProposal|CalendarTurn|CalendarModel|LegacyScheduleModelHost' \
  crates --glob '*.rs'
~~~

Classify every match.

Expected baseline classification:

- definitions: \`schedule/agent.rs\`, \`schedule/legacy_model.rs\`;
- tests/fixtures: \`schedule/agent/tests.rs\`, \`vault_host/tests/proposals.rs\`;
- production product caller: **0**.

Also verify:

~~~sh
rg -n \
  'run_calendar_agent_turn|CalendarAgentTurnRequest|CalendarTurn|LegacyScheduleModelHost' \
  apps server crates/bindings --glob '*'
~~~

Expected: no product/FFI/Flutter/server caller.

If execution HEAD has gained a real production caller since \`8bdd506...\`, do not blindly delete it. Record the caller as a residual blocker and migrate it to the canonical Schedule endpoint inside this residual slice if it is still a Schedule/domain caller. Do not push it to 3-D merely to preserve the old runtime.

Record the warning baseline before edits.

---

## R1 — Delete the caller-zero Schedule agent-turn runtime

### R1.1 \`schedule/agent.rs\`

Preserve:

- \`CalendarExpertEndpointRequest\`;
- \`CalendarExpertEndpointResult\`;
- \`CALENDAR_EXPERT_SETTLEMENT_OWNER\`;
- \`run_calendar_expert_endpoint\`;
- \`GrantBoundCalendarAccess\`;
- canonical Calendar source/view validation used by the endpoint;
- \`ExpertTimelineViews\`;
- helpers used by the canonical endpoint.

Delete the caller-zero agent-turn branch and direct fallout, including when no longer used:

- \`CalendarAgentTurnRequest\`;
- \`CalendarAgentProposal\`;
- \`CalendarAgentTurnResult\`;
- \`run_calendar_agent_turn\`;
- \`CalendarTurn\`;
- its \`SessionStore\`, \`CapabilityHost\`, \`InProcessAgent\` implementations;
- \`CalendarModel\`;
- old continuation/session-history/model-runtime helpers used only by that path;
- old event emission / \`CancelTurn\` / budget helpers used only by that path;
- old imports for \`AgentRuntime\`, legacy \`ModelRunner\`, \`TransportModelRunner\`, legacy model request/response, detached model usage, and agent-turn-only A2A/session values.

Do not remove an import/helper until compiler or exact caller search proves it belongs only to the deleted branch.

### R1.2 Delete the quarantine instead of keeping it

Delete:

~~~text
crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule/legacy_model.rs
~~~

and remove the module declaration from:

~~~text
crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule.rs
~~~

After deletion, the Schedule production tree must contain no:

- \`LegacyScheduleModelHost\`;
- \`TransportModelRunner\`;
- \`generate_with_recovery\`;
- detached \`UsageLedger\` used for model attempts;
- raw \`ModelTransportRequest\`;
- provider-selected model placement.

Do **not** replace \`legacy_model.rs\` with another test-support production module.

### R1.3 Preserve the canonical endpoint unchanged unless necessary

The residual is deletion-first.

Do not refactor \`run_calendar_expert_endpoint\` merely because adjacent legacy code disappears. Only make narrow compile/test fallout changes required to keep:

- Task/Expert child \`ExecutionScope\`;
- Context source acquisition;
- Access revalidation;
- exact dependency coverage;
- Inference-owned model execution;
- Expert settlement

working exactly as they do at \`8bdd506...\`.

---

## R2 — Move or retire old-path regressions

Deleting an obsolete runtime is not permission to lose durable architecture coverage.

### R2.1 Classify every old agent-turn test

In:

~~~text
crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule/agent/tests.rs
~~~

classify tests using \`run_calendar_agent_turn\` into these buckets.

#### A. Canonical Schedule endpoint invariant

Port to \`run_calendar_expert_endpoint\` / canonical \`InferenceExecutor\` fixtures when the assertion is still a Schedule owner invariant, including examples such as:

- grant/view disabled before execution → no model/source dispatch;
- registry/assignment revocation during generation wins;
- source/grant identity or revision mismatch fails closed;
- grant pause/scope expansion after a successful read blocks later egress;
- proposal/Expert result cannot publish after binding revocation;
- stale connection revision blocks publication;
- cancellation/deadline and exact settlement;
- source dependency coverage/liveness.

Prefer extending the endpoint-focused fixtures already present in this file rather than recreating a root Session runtime.

#### B. Stage 2 Conversation/session invariant

If the test exists only to prove:

- Conversation continuation/history projection;
- root Session model attempts/accounting;
- Manager delegation exchange persistence;
- root AgentRuntime halt/continuation semantics,

do not reproduce those semantics in Schedule. Confirm equivalent canonical Conversation/Task coverage already exists in Stage 2 tests; delete the Schedule duplicate. If a genuinely missing invariant is found, add the regression to the owning Conversation/Experts/Vault test, not a replacement Calendar runtime.

#### C. Action/proposal invariant

If the old turn is used only to manufacture a persisted Schedule Expert result for action/proposal tests, replace the seeding method rather than preserving the turn.

### R2.2 Refactor \`vault_host/tests/proposals.rs\` away from model execution

The current \`seed(...)\` uses:

~~~text
ModelTransport fixture
→ run_calendar_agent_turn
→ Session Delegation message
→ ExpertResult
~~~

The proposal test is not testing model routing. It is testing inspection/publication/revocation over a persisted Expert result.

Use the established test-only pattern in:

~~~text
crates/app/src/vault_host/tests/expert_actions.rs
~~~

which already builds a realistic completed A2A Task / \`AgentMessage::Delegation\`, stores the \`EXPERT_RESULT_MEDIA_TYPE\` payload and commits the session without requiring a production legacy model runtime.

Prefer one of:

1. factor a **test-only** shared helper under the existing vault-host test module; or
2. reproduce the minimal persisted Task/ExpertResult fixture locally in \`proposals.rs\`.

Requirements:

- no new public production API solely for test seeding;
- preserve realistic Task id / invocation id / artifact media type;
- preserve the exact session/reference shape that \`ExpertActionStore\` reads;
- preserve proposal inspect/publish/revocation assertions;
- remove \`ModelTransport\`, \`ModelTransportRequest\`, \`CalendarAgentTurnRequest\`, legacy prompt/model fixtures from \`proposals.rs\`.

### R2.3 Do not mechanically preserve every old test

For each deleted old-turn test, record one of:

~~~text
ported → canonical Schedule endpoint
covered → existing owner test <path/test>
moved  → owning module regression
removed → tests behavior of deleted compatibility only
~~~

This mapping belongs in the final residual Agent report.

The goal is invariant coverage, not preserving the count or shape of old tests.

---

## R3 — Static closure proof

After R1/R2, the following production symbols must be absent:

~~~sh
rg -n \
  'run_calendar_agent_turn|CalendarAgentTurnRequest|CalendarAgentTurnResult|CalendarAgentProposal|CalendarTurn|CalendarModel|LegacyScheduleModelHost' \
  crates/app/src --glob '*.rs'
~~~

Expected: **no match**.

Schedule production model-runtime gate:

~~~sh
rg -n \
  'TransportModelRunner|generate_with_recovery|ModelTransportRequest|ModelTransport|UsageLedger' \
  crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule \
  --glob '*.rs'
~~~

Expected: no production Schedule match. Test-only low-level fixtures should also disappear if they existed solely for the deleted path.

Module gate:

~~~sh
test ! -e crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule/legacy_model.rs
~~~

Canonical endpoint must remain:

~~~sh
rg -n 'run_calendar_expert_endpoint' \
  crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule.rs \
  crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule/agent.rs
~~~

Do not use repo-global \`generate_with_recovery\` = 0 as a 3-A gate: AgentFixture remains a named 3-D/3-E live production debt.

Also prove that A0/A1/A3 closure did not regress:

- \`InferenceService : ModelPort\` still present;
- \`InferenceExecutor\` still has one production implementation;
- Knowledge production has no \`InferenceRouter::new\` or raw \`ModelTransportRequest\`;
- built-in Expert production has no \`server_model()\` / \`device_model()\`;
- canonical \`PreparedFoundationTransport\` still reaches native transport.

---

## R4 — Validation and final 3-A freeze

### Focused tests

At minimum run:

~~~sh
cargo test -p floe-agent-contract
cargo test -p floe-inference
cargo test -p floe-provider-adapters
cargo test -p floe-context
cargo test -p floe-access
cargo test -p floe-experts
cargo test -p floe-experts-builtin
cargo test -p floe-actions
cargo test -p floe-vault
cargo test -p floe-conversation
cargo test -p floe-app
~~~

The exact package list may include additional directly changed owner crates. Do not omit a changed crate's tests.

### Full gate

~~~sh
cargo check --workspace --lib
cargo test --workspace --no-fail-fast
python3 tools/architecture/check_boundaries.py
git diff --check
~~~

Required:

- zero workspace failures;
- architecture \`errors == []\`;
- architecture \`warnings == []\`;
- \`git diff --check\` clean;
- no new warning introduced by the residual closure without an explicit reason.

Known Calendar mock-socket \`WouldBlock\` handling is unchanged: reproduce the exact failure alone and against the residual baseline; do not add sleeps/retries or weaken assertions. A final green full workspace run is still required.

### Documentation closure

Only after R0-R4 are green:

1. set this document back to \`complete · frozen\`;
2. append the **Residual closure Agent report** below the first-pass report;
3. update \`docs/refactoring/stage-3.md\`:
   - check 3-A;
   - Current checkpoint → \`3-B — AppHost Composition Closure\`;
   - state that the caller-zero Schedule agent-turn runtime was deleted;
   - make the pre-3A “Reviewed remaining debt” inventory explicitly historical or replace it with current remaining Stage 3 debt;
   - remove any current-state claim that \`PreparedFoundationTransport\` still returns \`LocalModelUnavailable\` or that Knowledge/Expert/Schedule production inference remains noncanonical.
4. stop. Do not start 3-B implementation in the same change set.

### Residual acceptance

3-A may be frozen only when:

- [ ] Schedule has exactly one production model-execution path: the canonical Expert endpoint;
- [ ] \`run_calendar_agent_turn\` and all caller-zero runtime types are deleted;
- [ ] \`schedule/legacy_model.rs\` is deleted;
- [ ] no production Schedule model attempt uses legacy \`TransportModelRunner\`, \`generate_with_recovery\` or detached \`UsageLedger\`;
- [ ] high-value grant/revocation/proposal/coverage invariants remain covered through canonical owners;
- [ ] proposal tests no longer need a production model runtime to seed Expert evidence;
- [ ] AgentFixture is the only intentionally retained App legacy model-runtime product debt, plus any other explicitly proven live 3-D/3-E caller;
- [ ] A0/A1/A3 gates remain intact;
- [ ] full validation is green;
- [ ] Stage 3 overview describes current reality.

### Residual closure Agent report format

Report only:

1. residual execution baseline and changed files/symbols;
2. R0 exact caller proof;
3. deleted Schedule agent-turn/runtime symbols;
4. old test mapping: ported / covered / moved / removed;
5. proposal fixture migration and preserved action/revocation assertions;
6. R3 static closure results;
7. A0/A1/A3 non-regression proof;
8. focused tests;
9. workspace/architecture/\`git diff --check\`;
10. warning/flaky baseline result if applicable;
11. remaining live legacy model-runtime callers deferred to 3-D/3-E;
12. residual blocker, if any;
13. final 3-A / Stage 3 checkpoint state.

---


## Goal

Converge every live production model-execution caller left outside the Stage 2 General Conversation cutover onto **one Inference-owned provider/route/attempt/usage path** while preserving each domain's own reasoning semantics.

The reviewed production debt is exactly three caller clusters:

1. delegated built-in Experts;
2. Schedule / Calendar;
3. Knowledge Learner.

Before those callers move, canonical Foundation inference must use the already-existing bundled native model instead of returning `LocalModelUnavailable`.

3-A changes model-execution ownership. It does not reopen the Stage 2 Manager runtime and it does not start AppHost/FFI/Flutter product-boundary work.

## Frozen foundation

Do not redesign or weaken:

- Conversation Run/transcript/continuation/finalization;
- Agent Engine validated-batch durability and cursor replay;
- exact Tool invocation identity;
- delegation request digest / execution-context binding;
- TaskCoordinator TaskId / InvocationKey / single settlement;
- Context dependency coverage and history reauthorization;
- ContextToolService;
- Access exact-recipient admit → consume → post-response revalidation;
- Inference attempt settlement and Auto/explicit fallback rules;
- `ExecutionScope` parent → child cancellation direction;
- durable pre-dispatch intent;
- uncertain external-write recovery.

If a remaining caller does not fit the canonical owner contracts, adapt the caller or add the narrow owner-facing contract required by that caller. Do not create a second model runtime.

---

## Fixed 3-A design

### One Inference execution implementation

`InferenceService` remains the only production owner of:

- profile observation and candidate selection;
- execution-location constraint evaluation;
- Access model-dispatch admit/consume/revalidate;
- provider handoff;
- attempt identity;
- model budget settlement;
- transport fallback;
- trustworthy usage accounting.

The current `ModelPort` implementation is root-specific because it pins `CANONICAL_MODEL_PURPOSE` and `CANONICAL_MODEL_CONSUMER`. Preserve that root invariant.

Extract/generalize the body of `InferenceService::generate_inner` into one Inference-owned execution entry, then keep `impl ModelPort for InferenceService` as the Conversation-root adapter.

Use an owner-level constraint shaped like:

~~~rust
pub enum InferenceExecutionConstraint {
    Any,
    DeviceOnly,
    RemoteOnly,
}
~~~

and one shared execution entry, for example:

~~~rust
InferenceService::execute(
    ModelRequest,
    &ExecutionScope,
    InferenceExecutionConstraint,
) -> Result<ModelResponse, AgentFailure>
~~~

Exact naming/visibility may vary if a narrower Inference trait is cleaner, but there must be only one implementation of candidate planning, Access fencing, attempt lifecycle, fallback and settlement.

Do not introduce `InferenceServiceV2`, `DomainInferenceService`, a second router loop or another usage ledger.

### Domain callers express constraints, not routes

Semantic mapping:

~~~text
General Conversation       → Any
Focus/device-only Expert   → DeviceOnly
remote-only built-in       → RemoteOnly
Schedule OnDevice          → DeviceOnly
Schedule ConversationRoute → Any
Knowledge Learner          → DeviceOnly
~~~

A caller must not construct `RemoteRoute`, `candidate_route`, `FoundationModelRunner`, `ServerModelRunner`, endpoint/token or a chosen provider transport.

Inference selects a profile satisfying request purpose/consumer plus the execution constraint.

### Preserve purpose/consumer meaning

Only the root `ModelPort` adapter pins `conversation.root`.

The shared path accepts domain purpose/consumer. Knowledge retains:

~~~text
purpose  = governed-memory-review
consumer = knowledge.learner
~~~

Do not label every domain call `conversation.root` simply to reuse the root profile.

Provider adapters must not import Expert or Knowledge crates. If domain-specific profile scoping is needed, implement it in Inference/composition over non-secret profile facts.

### ExecutionScope is the sole model budget owner

Delegated endpoints already receive an `ExecutionScope` from TaskCoordinator.

Expert/Schedule calls derive bounded child scopes from that parent. Their max tokens/cost/deadline remain domain limits, but model attempts settle against the shared execution ledger.

The Learner is background work, not a child of the foreground turn. Convert `LearnerBudget` into a background root `ExecutionScope`; Inference owns attempts inside it.

Do not retain detached `UsageLedger` model accounting after cutover.

### Expert placement is intent

Current provider preselection leaks through:

- `ExpertModel::placement()`;
- `BuiltinExpertHost::server_model()`;
- `BuiltinExpertHost::device_model()`;
- `Model::expert_eligibility()`.

Replace this with an Expert-owned requirement such as:

~~~rust
pub enum ExpertModelRequirement {
    Any,
    DeviceOnly,
    RemoteOnly,
}
~~~

carried by `ExpertModelCall` / `ExpertReasoningStep` or an equivalent contract.

Experts may state what execution class they require; they must not inspect which provider Inference chose.

### Reuse Context projection

Use the existing Context-owned `ContextProjectionInput` and `assemble_context_projection`.

Extend projection roles only as narrowly as needed for Expert/Learner use. Map Expert transcript to `ModelConversation`, Expert capabilities to canonical `AllowedCatalog`, and preserve exact source dependency coverage.

Do not recreate the old Conversation `ModelRequest::context_envelope()` path in App.

Do not put route, endpoint, credential, recipient or transfer-consent policy into the projection. Access remains the model-recipient authority after Inference selects a candidate.

Source-backed Expert input must not become `DependencyCoverage::Independent`.

---

# A0 — Canonical Foundation transport

## A0.0 Revalidate baseline

Before edits:

- [ ] confirm HEAD descends from the planning baseline;
- [ ] confirm Stage 2 remains complete/frozen;
- [ ] record current production callers of `PreparedFoundationTransport`, `FoundationModelRunner`, `TransportModelRunner`, `ModelTransportRequest`, `generate_with_recovery`, `InferenceRouter::new`;
- [ ] record pristine workspace warning baseline.

Do not start A1 until A0 canonical transport tests are green.

## A0.1 Reuse native primitives without adapting back to legacy transport

Primary file:

- `crates/adapters/providers/src/models/foundation.rs`.

Current legacy path:

~~~text
FoundationModelRunner::generate(ModelTransportRequest)
→ prepare
→ Lease
→ NativeTransport/native_call
→ libfloe_local_model.dylib
→ decode_step
~~~

Current canonical path:

~~~text
PreparedFoundationTransport::generate(CanonicalModelRequest)
→ LocalModelUnavailable
~~~

Required changes:

- [ ] keep `Transport`, `NativeTransport`, `Lease`, `native_call` and native failure mapping as private reusable primitives;
- [ ] add canonical input preparation from `CanonicalModelRequest`;
- [ ] add a canonical native-generation helper testable with mock `Transport`;
- [ ] never convert `CanonicalModelRequest` into legacy `ModelTransportRequest`;
- [ ] render instructions/context from canonical `ContextEnvelope`;
- [ ] derive callable tools/delegates only from canonical `AllowedCatalog`;
- [ ] map returned tool/agent IDs to the exact catalog `definition_revision`;
- [ ] reject unknown IDs, malformed input/context refs and oversized output before returning to Inference.

For the canonical path, use `CanonicalModelRequest.attempt_id` as the native start/poll/release request identity. Do not generate a second UUID for the same model attempt.

Legacy `FoundationModelRunner` may remain temporarily while real production callers still exist.

## A0.2 Preserve native behavior

Canonical Foundation must preserve:

- [ ] cancellation before handoff and during polling;
- [ ] deadline enforcement;
- [ ] `SessionProtection`;
- [ ] bounded prompt/context/output;
- [ ] native availability mapping;
- [ ] release semantics;
- [ ] local invalid-output failure class;
- [ ] no provider-internal remote fallback.

No Access/Context policy moves into Swift.

## A0.3 Tests and gate

Add/update canonical provider tests through mock native `Transport` for:

- [ ] Answer;
- [ ] Tool call with exact revision;
- [ ] Delegate with exact agent revision/context refs;
- [ ] unknown tool/agent fail closed;
- [ ] attempt ID reaches native request ID;
- [ ] cancellation/deadline;
- [ ] invalid/oversized output;
- [ ] unavailable native model;
- [ ] release.

Run:

~~~sh
cargo check -p floe-provider-adapters
cargo test -p floe-provider-adapters
cargo test -p floe-inference
~~~

A0 does not require a FoundationModels-capable machine. Packaged Apple evidence remains 3-D/3-F.

---

# A1 — Shared Inference entry and delegated built-in Experts

## A1.1 Generalize InferenceService, preserve root behavior

Primary files:

- `crates/modules/inference/src/api.rs`;
- `crates/modules/inference/src/application/service.rs`;
- `crates/modules/inference/src/application/mod.rs`;
- `crates/modules/inference/src/lib.rs`.

Changes:

- [ ] add/export execution constraint;
- [ ] add shared Inference execution entry;
- [ ] apply constraint inside candidate eligibility;
- [ ] keep explicit-profile exact/no-fallback semantics;
- [ ] keep Auto local-first/same-rank ambiguity behavior;
- [ ] keep admission-denial no-fallback behavior;
- [ ] keep `attempt_candidate` as the sole Access/budget/provider path;
- [ ] keep trustworthy usage settlement exactly once.

`impl ModelPort for InferenceService` must still reject non-root purpose/consumer or projection-purpose mismatch, then call the shared path with `Any`.

Add regression coverage proving Stage 2 General Conversation model behavior is unchanged.

## A1.2 Make Expert contract intent-oriented

Primary files:

- `crates/contracts/agent/src/expert_model.rs`;
- `crates/contracts/agent/src/lib.rs`;
- `crates/experts/builtin/src/host.rs`;
- built-in dispatches/tests.

Changes:

- [ ] add/use Expert model requirement intent;
- [ ] remove production `ExpertModel::placement()`;
- [ ] remove `BuiltinExpertHost::server_model()` / `device_model()`;
- [ ] retain one model/reasoner dependency;
- [ ] Focus → DeviceOnly;
- [ ] server-required Communication/Work Context/Life Logistics/Commitments → RemoteOnly;
- [ ] Experts supporting both → Any;
- [ ] carry requirement on model call/reasoning step.

If `InferencePolicyDecision` remains temporarily for Context source preparation, keep it only for Context/source semantics. It must not select provider placement or authorize model transfer after canonical Inference/Access takes over.

## A1.3 Rebuild ExpertModelHost on canonical contracts

Primary file:

- `crates/app/src/vault_host/conversation_turn/expert_compat.rs`.

Replace:

~~~text
Model enum
→ ModelTransport
→ TransportModelRunner
→ generate_with_recovery
→ detached UsageLedger
~~~

with:

~~~text
Expert call/transcript
→ Context-owned AuthorizedModelProjection
→ floe_agent_contract::ModelRequest
→ bounded child ExecutionScope
→ InferenceService shared execution
→ ExpertModelAnswer / ExpertStepOutcome
~~~

Rules:

- [ ] single-answer Expert accepts exactly one answer;
- [ ] reasoning Expert accepts only declared capabilities;
- [ ] Expert delegation output remains denied;
- [ ] no detached model usage ledger;
- [ ] no fabricated provider replay IDs;
- [ ] source-derived coverage remains exact.

If canonical Inference makes old Expert provider-replay fields meaningless, remove them from the Expert model contract/tests rather than synthesizing compatibility values.

## A1.4 Compose Inference into delegated endpoint

Primary files:

- `crates/app/src/vault_host/conversation_turn/expert_dispatch.rs`;
- `crates/app/src/vault_host/conversation_turn.rs`;
- current root provider/recipient/dependency composition helpers.

`BuiltinExpertEndpoint::execute` already receives TaskCoordinator's `ExecutionScope`. Pass the Inference executor and that scope into the Expert host.

Remove delegated built-in production use of:

- [ ] `Model::for_stored_connection`;
- [ ] `Model::expert_eligibility`;
- [ ] `FoundationModelRunner`;
- [ ] `ServerModelRunner`;
- [ ] `candidate_route`;
- [ ] `TransportModelRunner`;
- [ ] `generate_with_recovery`;
- [ ] `UsageLedger` as attempt owner.

Do not alter TaskCoordinator/Directory/Task admission or settlement.

## A1.5 Static/focused gate

After A1, excluding Schedule until A2:

~~~sh
rg -n \
  'Model::for_stored_connection|expert_eligibility|FoundationModelRunner|ServerModelRunner|candidate_route|TransportModelRunner|generate_with_recovery|ModelTransportRequest' \
  crates/app/src/vault_host/conversation_turn/expert_compat.rs \
  crates/app/src/vault_host/conversation_turn/expert_dispatch.rs
~~~

Expected: no delegated built-in Expert production execution match.

Also prove built-in production dispatch contains no provider-selecting `server_model()` / `device_model()`.

Run:

~~~sh
cargo check -p floe-agent-contract
cargo test -p floe-agent-contract
cargo check -p floe-experts
cargo test -p floe-experts
cargo check -p floe-experts-builtin
cargo test -p floe-experts-builtin
cargo check -p floe-app
cargo test -p floe-app
~~~

---

# A2 — Schedule / Calendar convergence

Primary files:

- `crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule.rs`;
- `crates/app/src/vault_host/conversation_turn/expert_dispatch/schedule/agent.rs`;
- its tests;
- `crates/experts/builtin/src/schedule/*`.

Schedule remains domain-owned reasoning. It stops being a second model runtime.

## A2.1 Replace model construction with intent

Current:

~~~text
ScheduleRunPlan.reasoning
→ Model::for_stored_connection
→ Foundation/Server runner
→ run_calendar_expert_endpoint
~~~

Target:

~~~text
ScheduleRunPlan.reasoning
→ InferenceExecutionConstraint
   OnDevice          → DeviceOnly
   ConversationRoute → Any
→ canonical Inference
~~~

`ScheduleReasoning` may remain if it is useful domain intent, but it must not contain/construct provider route data.

## A2.2 Remove Calendar-owned model runtime/accounting

Remove production dependence on:

- [ ] `CalendarTurn` as a parallel SessionStore/CapabilityHost/InProcessAgent runtime;
- [ ] `CalendarModel : ModelRunner`;
- [ ] `TransportModelRunner`;
- [ ] legacy `ModelRunner`;
- [ ] legacy `generate_with_recovery`;
- [ ] Calendar-local model `UsageLedger`;
- [ ] placement-derived model policy such as `remote_processing`.

Preserve Schedule transcript/reasoning, bounded capability-call count, source/grant validation, proposal semantics and Action uncertainty/recovery.

Use the Task/Expert `ExecutionScope` child for model budget/cancellation.

## A2.3 Put Schedule source access behind Context/Access

`schedule.rs` contains the explicit Stage 3 provider-adapter boundary.

For Schedule-specific source acquisition:

~~~text
Schedule → required View/query
Context  → acquisition/dependency
Access   → grant/current authority
Adapter  → endpoint/credential HTTP
~~~

Remove Schedule production construction/use of `ServerSourceClient::prepare`, `RemoteAuthorizationClient` and provider route details when they perform source-policy work.

Do not broaden A2 into the generic General Conversation App source seam; that is 3-B.

## A2.4 Canonicalize production endpoint first

Canonicalize `run_calendar_expert_endpoint` first.

Then re-run callers for:

- `CalendarTurn`;
- `CalendarAgentTurnRequest`;
- `run_calendar_agent_turn`;
- `run_calendar_expert_endpoint`.

Delete only caller-zero direct fallout. Product/fixture callers outside the production endpoint are explicitly deferred to 3-D/3-E.

## A2.5 Regressions/gate

Preserve/add:

- [ ] local-only Schedule reasoning;
- [ ] paired/approved remote source;
- [ ] denied/revoked source;
- [ ] source identity/revision mismatch;
- [ ] history revocation during model call;
- [ ] bounded capability calls;
- [ ] proposal idempotency;
- [ ] cancellation/deadline;
- [ ] Task settlement once;
- [ ] no model/provider dispatch before required source/grant admission.

Run:

~~~sh
cargo test -p floe-experts-builtin
cargo test -p floe-experts
cargo test -p floe-context
cargo test -p floe-access
cargo test -p floe-app
~~~

---

# A3 — Knowledge Learner convergence

Primary files:

- `crates/modules/knowledge/src/application/inference.rs`;
- `crates/modules/knowledge/src/application/learner_transport.rs`;
- `crates/modules/knowledge/src/application/learner_service.rs`;
- `crates/modules/knowledge/src/application/learner.rs`;
- `crates/app/src/vault_host/learner_worker.rs`;
- `crates/adapters/providers/src/models/learner.rs`;
- affected Learner tests/examples.

Knowledge keeps an isolated background Learner. It stops owning routing/raw transport.

## A3.1 Preserve Knowledge-owned semantics

Keep:

- `LearnerBudget`;
- eligibility/job lease/retry;
- bounded digest/evidence;
- learner prompt;
- `LEARNER_INFERENCE_PURPOSE`;
- `LEARNER_INFERENCE_CONSUMER`;
- device-only constraint;
- no network/domain Tool/delegation;
- pending candidate staging;
- Review boundary;
- candidate idempotency.

Do not merge the Learner into foreground Conversation.

## A3.2 Remove Knowledge routing/transport ownership

Delete production ownership of:

- [ ] `InferenceRouter::new` in Knowledge;
- [ ] direct Learner `RouteRequest` planning when shared Inference can do it;
- [ ] direct `ModelTransportRequest` construction;
- [ ] random attempt UUID created in `review_with_model`;
- [ ] raw `ModelTransport::generate`.

Knowledge builds bounded input/projection and asks Inference for:

~~~text
purpose    = governed-memory-review
consumer   = knowledge.learner
constraint = DeviceOnly
catalog    = no domain Tools / no delegates
~~~

## A3.3 Convert LearnerBudget to background ExecutionScope

Create one background root `ExecutionScope` from:

- max model tokens;
- max model cost;
- learner deadline;
- learner cancellation;
- stable Learner run/job trace identity where available.

Inference is the sole attempt/usage owner in that scope.

Do not keep a second transport usage counter and reconcile it later.

## A3.4 Remove legacy learner transport chain

Move composition away from:

~~~text
FoundationLearnerTransport
→ BundledLearnerModel
→ TransportLearnerModel
→ raw ModelTransport
~~~

toward canonical Foundation prepared transport + shared Inference execution.

Provider adapters must not import Knowledge constants.

Delete learner-specific wrappers only when caller count is zero; otherwise classify them for A4/3-E.

## A3.5 Regressions/gate

Preserve/add:

- [ ] ineligible job → no model call;
- [ ] cancellation before call;
- [ ] deadline during call;
- [ ] device profile eligible;
- [ ] remote/external profile denied;
- [ ] Tool/delegation output denied;
- [ ] malformed/oversized output denied;
- [ ] retry/restart does not duplicate candidate;
- [ ] no foreground Run mutation;
- [ ] attempt/usage charged once by Inference.

Run:

~~~sh
cargo check -p floe-knowledge
cargo test -p floe-knowledge
cargo check -p floe-app
cargo test -p floe-app
cargo test -p floe-provider-adapters
cargo test -p floe-inference
~~~

---

# A4 — Caller-zero cleanup and 3-A closure

## A4.1 Re-run caller inventory

Classify every remaining production/test/provider match for:

- `ModelTransport`;
- `ModelTransportRequest`;
- `FoundationModelRunner`;
- `ServerModelRunner`;
- `TransportModelRunner`;
- `generate_with_recovery`;
- `AgentRuntime`;
- `CapabilityHost`;
- `ModelRunner`;
- `InferencePolicyDecision`;
- `candidate_route`;
- `CalendarTurn`;
- `run_calendar_agent_turn`;
- `FoundationLearnerTransport`;
- `TransportLearnerModel`.

Classification:

1. **3-A production caller** → blocker; finish convergence.
2. **AgentFixture/product-boundary caller** → explicitly defer to 3-D/3-E.
3. **provider implementation internal** → keep when backing canonical transport.
4. **test/example** → update deleted behavior; retain genuine low-level validation.
5. **zero caller** → delete when direct A0-A3 fallout; otherwise list for 3-E.

Do not retain production compatibility solely because tests import it.

## A4.2 Static outcome

- [ ] Expert/Schedule/Knowledge production does not construct `ModelTransportRequest`;
- [ ] Expert/Schedule does not select Foundation/Server or call `candidate_route`;
- [ ] Knowledge production does not instantiate `InferenceRouter`;
- [ ] remaining `generate_with_recovery` / `AgentRuntime` production callers are explicitly AgentFixture/product-boundary debt or another named deferred caller;
- [ ] provider adapters have no Expert/Knowledge dependency;
- [ ] General Conversation still uses `InferenceService : ModelPort`.

## A4.3 Full validation

Run:

~~~sh
cargo check -p floe-agent-contract
cargo test -p floe-agent-contract
cargo check -p floe-inference
cargo test -p floe-inference
cargo check -p floe-provider-adapters
cargo test -p floe-provider-adapters
cargo check -p floe-context
cargo test -p floe-context
cargo check -p floe-access
cargo test -p floe-access
cargo check -p floe-experts
cargo test -p floe-experts
cargo check -p floe-experts-builtin
cargo test -p floe-experts-builtin
cargo check -p floe-knowledge
cargo test -p floe-knowledge
cargo check -p floe-app
cargo test -p floe-app

cargo check --workspace --lib
cargo test --workspace --no-fail-fast
python3 tools/architecture/check_boundaries.py
git diff --check
~~~

Required:

- zero failed workspace tests;
- architecture errors `[]`;
- architecture warnings `[]`;
- no new warning without an explicit retained reason;
- `git diff --check` clean.

Known Calendar mock-socket `WouldBlock`: reproduce alone and compare to the execution baseline; do not add sleeps or weaken assertions. A final green full workspace run is still required.

---

## Explicit non-goals

Do not in 3-A:

- change Flutter protocol DTOs;
- delete `AgentRemoteRouteDto`;
- delete AppHost `legacy_services()`;
- redesign pairing/grant UI;
- remove Flutter bearer route maps;
- repair unrelated App composition seams assigned to 3-B;
- perform 3-C/3-D ABI cutover;
- perform final Apple product acceptance;
- increase schema versions;
- add permanent compatibility wrappers;
- reopen Stage 2 batch/recovery/access architecture;
- make server authorization replace local Access;
- make provider adapters depend on Expert/Knowledge domain crates.

---

## Acceptance

3-A is complete only when:

- [ ] `PreparedFoundationTransport` uses the existing native Foundation transport through canonical request/response;
- [ ] canonical native request identity equals model attempt identity;
- [ ] one Inference implementation owns provider selection, Access fencing, fallback, attempt and usage settlement for root + Expert + Schedule + Learner;
- [ ] root `ModelPort` semantics remain unchanged;
- [ ] built-in Experts express placement as intent and no longer preselect `server_model()` / `device_model()`;
- [ ] delegated Expert execution has no Foundation/Server selection or detached model ledger;
- [ ] Schedule has no parallel model runtime/accounting or provider-route selection;
- [ ] Knowledge Learner keeps isolated policy but no longer owns `InferenceRouter` or raw `ModelTransportRequest`;
- [ ] Expert Task identity/coverage/provenance/cancellation remain intact;
- [ ] no 3-A production caller reconstructs endpoint/bearer/model route;
- [ ] remaining legacy runtime caller is explicitly classified as AgentFixture/product-boundary debt for 3-D/3-E;
- [ ] all focused/workspace/architecture/diff gates are green.

When complete:

1. set this document to `complete · frozen`;
2. append the Agent report below;
3. check 3-A in `docs/refactoring/stage-3.md`;
4. move Current checkpoint to `3-B — AppHost Composition Closure`;
5. stop. Do not start 3-B implementation in the same change set.

## Agent report format

Report only:

1. execution baseline and changed files/symbols by A0-A4;
2. final shared Inference execution contract and root `ModelPort` compatibility;
3. canonical Foundation transport and attempt-identity proof;
4. delegated built-in Expert contract/caller convergence;
5. Schedule/Calendar model/source convergence;
6. Knowledge Learner convergence and preserved isolation/budget semantics;
7. remaining legacy runtime symbols classified by production/fixture/test/provider caller;
8. static caller-gate results;
9. focused test results;
10. workspace check/test + architecture checker + `git diff --check`;
11. warning/flaky baseline result if applicable;
12. remaining 3-A blocker, if any;
13. final document/checkpoint state.

## Agent report

1. Execution baseline and changed files/symbols by A0-A4.
   - Baseline: HEAD `cea4870` verified to descend from planning baseline `170770d`; code baseline `39263a1` (`2-F`). Stage 2 left untouched/frozen. Total: 51 files, +4196/−2004.
   - A0 (canonical Foundation transport): `adapters/providers/src/models/foundation.rs` (`PreparedFoundationTransport::generate` over private `Transport`/`NativeTransport`/`Lease`/`native_call`, canonical input preparation, `attempt_id` as native request identity), `root.rs`/`server.rs` (domain-scoped observation), `platform/native/src/dylib.rs` (honest native availability).
   - A1 (shared Inference entry + delegated Experts): `modules/inference/src/api.rs` (`InferenceExecutionConstraint`), `application/service.rs` (`InferenceExecutor::execute` + `generate_inner`, `ModelPort` root adapter), `lib.rs`/`application/mod.rs` (exports); `contracts/agent/src/expert_model.rs` (`ExpertModelRequirement`, placement removal); `experts/builtin` (`host.rs`, `shared.rs`, per-expert `dispatch.rs`/`expert.rs`, requirement mapping); `app/.../expert_compat.rs` (`ExpertModelHost` bridge), `expert_dispatch.rs` (endpoint composition), `conversation_turn.rs` (composition), `modules/context/.../model_projection.rs` (`ContextProjectionRole::Expert`).
   - A2 (Schedule/Calendar): `app/.../expert_dispatch/schedule.rs` (intent → constraint), `schedule/agent.rs` + `agent/tests.rs` (endpoint canonicalization), `schedule/legacy_model.rs` (new; quarantined legacy host behind the deferred agent turn), `experts/builtin/src/schedule/*` (`host.rs`, `mod.rs`, `plan.rs` intent-only reasoning), `app/.../tests/calendar_experts.rs` (endpoint regressions).
   - A3 (Knowledge Learner): `modules/knowledge/.../learner.rs` (kept `LearnerBudget`/lease/retry/prompt/purpose/consumer semantics, `parse_learner_review_output`, moved runtime regressions), `lib.rs`/`Cargo.toml` (`floe-inference` dep removed); deleted `application/inference.rs` (`InferenceLearnerModel`, `InferenceRouter::new` planning), `application/learner_transport.rs` + `tests.rs` (`TransportLearnerModel`, `review_with_model`, `learner_profile`), `adapters/providers/src/models/learner.rs` (`FoundationLearnerTransport`); `modules/context/.../model_projection.rs` (`ContextProjectionRole::Learner`); `app/.../learner_worker.rs` (`LearnerModelHost` bridge + background root scope); `app/examples/local_model_smoke/learner.rs` (canonical smoke composition); `Cargo.lock`.
   - A4 (cleanup): `modules/inference/.../route_config.rs` + `application/mod.rs` + `lib.rs` (`candidate_route` deleted, zero callers); `calendar_experts.rs` EOF whitespace fix for `git diff --check`.

2. Final shared Inference execution contract and root `ModelPort` compatibility.
   - One owner-level entry: `InferenceExecutor::execute(ModelRequest, &ExecutionScope, InferenceExecutionConstraint)`, implemented only by `InferenceService`. `InferenceExecutionConstraint::{Any, DeviceOnly, RemoteOnly}` is applied inside candidate eligibility; `attempt_candidate` remains the sole Access/budget/provider path with exactly-once trustworthy settlement.
   - Root compatibility: `impl ModelPort for InferenceService` still rejects non-`conversation.root` purpose/consumer and projection-purpose mismatch, then calls the shared path with `Any`. Explicit-profile exact/no-fallback, Auto local-first/same-rank-denied, and admission-denial no-fallback semantics unchanged, with Stage 2 regression coverage intact.

3. Canonical Foundation transport and attempt-identity proof.
   - `PreparedFoundationTransport::generate(CanonicalModelRequest)` renders instructions/context from the canonical `ContextEnvelope`, derives callable tools/delegates only from the canonical `AllowedCatalog` with exact `definition_revision` mapping, rejects unknown IDs/malformed refs/oversized output, and drives the existing native transport; `CanonicalModelRequest.attempt_id` is the native start/poll/release request identity (no second UUID). Cancellation/deadline, `SessionProtection`, bounded I/O, native availability mapping, release semantics, local invalid-output class, and no provider-internal remote fallback are preserved. Provider tests cover Answer, revision-exact Tool/Delegate, fail-closed unknown IDs, attempt-ID propagation, cancellation/deadline, invalid/oversized output, unavailable model, and release.

4. Delegated built-in Expert contract/caller convergence.
   - Contract is intent-only: `ExpertModelRequirement::{Any, DeviceOnly, RemoteOnly}` carried on the model call/reasoning step; `ExpertModel::placement()`, `BuiltinExpertHost::server_model()`/`device_model()` removed. Focus → DeviceOnly, server-required Communication/Work Context/Life Logistics/Commitments → RemoteOnly, dual-capable → Any.
   - `ExpertModelHost` (App) projects via Context-owned `assemble_context_projection` (Expert role), builds `floe_agent_contract::ModelRequest`, derives a bounded child `ExecutionScope` from the Task scope, and executes through `InferenceExecutor`. Single-answer accepts exactly one answer; reasoning accepts only declared capabilities; delegation output denied; no detached usage ledger; no fabricated replay IDs; exact source coverage preserved. Endpoint composes `RootModelProvider::for_saved_connection_scoped` (expert consumer) with the Task scope. `InferencePolicyDecision` remains only for Context/source semantics.

5. Schedule/Calendar model/source convergence.
   - `ScheduleRunPlan.reasoning` maps to `InferenceExecutionConstraint` (OnDevice → DeviceOnly, ConversationRoute → Any) with no provider route data; `run_calendar_expert_endpoint` runs on the Task/Expert child `ExecutionScope` through shared Inference. Calendar-owned `CalendarModel`, local `UsageLedger`, and placement-derived policy removed from the endpoint; Schedule source acquisition stays behind the Context/Access/adapter seam.
   - Deferred to 3-D/3-E (named): `run_calendar_agent_turn` / `CalendarTurn` / `CalendarAgentTurnRequest` / `CalendarModel` and `schedule/legacy_model.rs` (`LegacyScheduleModelHost` quarantining `TransportModelRunner` + `generate_with_recovery` for the agent turn only), with `agent/tests.rs` and `tests/proposals.rs` as their fixtures. The production endpoint is canonical; only the agent turn still recovers through conversation's legacy adapter.

6. Knowledge Learner convergence and preserved isolation/budget semantics.
   - Removed: `InferenceRouter::new` in Knowledge, direct `RouteRequest` planning, direct `ModelTransportRequest` construction, the random attempt UUID in `review_with_model`, raw `ModelTransport::generate`, and the `FoundationLearnerTransport → BundledLearnerModel → TransportLearnerModel → raw ModelTransport` chain. Provider adapters import no Knowledge constants (scope strings pass from App composition).
   - `LearnerModelHost` (App) assembles a Context projection (Learner role, `governed-memory-review` / `knowledge.learner`, empty tool/delegate catalog, digest as the single turn, memories as live context, `DataClass::Personal`) and executes via `InferenceExecutor` with `DeviceOnly`, accepting exactly one structured Answer parsed by Knowledge-owned `parse_learner_review_output`.
   - Preserved: `LearnerBudget`, eligibility/job lease/retry, bounded digest/evidence, Learner prompt, purpose/consumer, device-only constraint, no Tools/delegates, pending staging, Review boundary, candidate idempotency, isolated background execution. Each review converts its `LearnerBudget` into one background root `ExecutionScope` (budget allowance, deadline, cancellation, stable run trace); Inference settles attempt/usage exactly once with no second counter. `LearnerModel::placement()` still reports `DeviceLocal` and the runtime still enforces it.

7. Remaining legacy runtime symbols classified by production/fixture/test/provider caller.
   - Production 3-A callers: none. No 3-A production code constructs `ModelTransportRequest`, selects Foundation/Server, calls `candidate_route` (deleted), or instantiates `InferenceRouter` outside Inference/review-time validation.
   - AgentFixture/product-boundary production debt (3-D/3-E): `AgentRuntime`, `CapabilityHost`, `ModelRunner`, `generate_with_recovery` via `app/src/agent_fixture.rs` (`run_persisted_agent_sample`, `recover_agent_sample`, driven from `vault_host`) and conversation's legacy `turn/runtime.rs` implementation; `TransportModelRunner` + `generate_with_recovery` via `schedule/legacy_model.rs` for the deferred Schedule agent turn only.
   - Provider implementation internal (kept): `ModelTransport`/`ModelTransportRequest` port + `FoundationModelRunner`/`ServerModelRunner` while legacy runners exist; canonical legs (`PreparedFoundationTransport`, `PreparedServerTransport`, `RootModelProvider`, `FoundationModelProvider::scoped`) back all 3-A production callers. Final runner removal listed for 3-E.
   - Test/example (genuine low-level validation retained): conversation `tests/turn_runtime.rs`, provider `models`/`sources` tests, `NoCapabilities` fixture support in `conversation_turn.rs`, smoke example manager `--exercise` path, deferred-path fixtures (`agent/tests.rs`, `tests/proposals.rs`).
   - Context/source-temporary (not placement/transfer): `InferencePolicyDecision` in `context/.../expert_context.rs`, Expert contract/policy plumbing, and builtin Expert dispatch (`policy.data_classes` → projection input only).
   - Zero-caller deletions done: `candidate_route`, `InferenceLearnerModel`, `LearnerInferenceTransport`, `LearnerInferenceResponse`, `TransportLearnerModel`, `review_with_model`, `learner_profile`, `LearnerModelAvailability`, `FOUNDATION_LEARNER_PROFILE`, `LEARNER_INFERENCE_CAPABILITY`, `FoundationLearnerTransport`.

8. Static caller-gate results.
   - Expert gate (`Model::for_stored_connection|expert_eligibility|FoundationModelRunner|ServerModelRunner|candidate_route|TransportModelRunner|generate_with_recovery|ModelTransportRequest` over `expert_compat.rs` + `expert_dispatch.rs`): no match. No `server_model()`/`device_model()` in builtin production dispatch.
   - Schedule endpoint + Learner + Knowledge gate (`ModelTransportRequest|FoundationModelRunner|ServerModelRunner|candidate_route|TransportModelRunner` over `schedule.rs`, `learner_worker.rs`, `modules/knowledge/src/`): no match.
   - Deferred agent-turn direct-construction gate (`ModelTransportRequest|FoundationModelRunner|ServerModelRunner|candidate_route` over `schedule/agent.rs` + `legacy_model.rs`): no match.
   - `InferenceRouter` over learner/expert/schedule production + Knowledge: no match. Provider `Cargo.toml`: no Expert/Knowledge dependency. Root path still constructs `InferenceService` (`conversation_turn.rs`) behind `ModelPort`.

9. Focused test results.
   - `floe-agent-contract`: 8 passed, 0 failed. `floe-inference`: 32 passed, 0 failed. `floe-provider-adapters`: 101 passed, 0 failed. `floe-context`: 102 passed, 0 failed (incl. new Learner-role projection test). `floe-access`: 29 passed, 0 failed. `floe-experts`: 19 passed, 0 failed. `floe-experts-builtin`: 70 passed, 0 failed. `floe-knowledge`: 24 passed, 0 failed (moved runtime regressions + strict parser tests). `floe-app`: 213 passed, 0 failed (206 lib incl. 6 new `LearnerModelHost` bridge tests, + vault Learner settlement and integration suites). Zero failures in every focused suite.

10. Workspace check/test + architecture checker + `git diff --check`.
    - `cargo check --workspace --lib`: green. `cargo test --workspace --no-fail-fast`: final run exit 0 — 1023 passed, 0 failed, 0 non-ok suites across 94 suites. `python3 tools/architecture/check_boundaries.py`: `errors: []`, `warnings: []` (22 nodes, 100 edges). `git diff --check`: clean.

11. Warning/flaky baseline result.
    - Warnings: no warning in any A3/A4-touched file (verified zero `learner_worker.rs` mentions under `--tests`); remaining warnings (`floe-app` 2 lib, plus pre-existing `floe-conversation`/`floe-ffi`/test-only notes) are retained pre-existing baseline items, none introduced by 3-A. Repo-wide `cargo fmt --check` drift (352 files, no `rustfmt.toml`) is pre-existing and not a 3-A gate; no formatting churn added.
    - Flaky: one transient single-test failure observed in 1 of 5 full workspace runs and 1 early calendar-alone run; the failing test name was not captured. 9 consecutive calendar-alone re-runs and 3 subsequent full workspace runs are green with no code changes, no sleeps, and no weakened assertions. Signature matches the plan's known Calendar mock-socket `WouldBlock` flake; final green full run obtained as required.

12. Remaining 3-A blocker, if any.
    - None. All acceptance criteria hold; every remaining legacy caller is classified above for 3-D/3-E or 3-E.

13. Final document/checkpoint state.
    - `docs/refactoring/stage-3/3-a.md`: `complete · frozen` with this report. `docs/refactoring/stage-3.md`: 3-A checked; Current checkpoint moved to `3-B — AppHost Composition Closure`. Stopped; 3-B not started.

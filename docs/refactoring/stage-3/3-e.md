# 3-E — Outer Compatibility Deletion

**Status: pending**

## Checklist
- [ ] delete `AgentRemoteRouteDto`;
- [ ] delete model-execution uses of `RemoteTurnRoute` where caller count is zero;
- [ ] delete legacy FFI route conversion;
- [ ] delete Flutter route/provider compatibility models;
- [ ] delete old worker variants and temporary protocol fields;
- [ ] delete AppHost constructors preserved only for old callers;
- [ ] delete obsolete execution-profile compatibility;
- [ ] delete `CalendarTurn` / run-calendar compatibility replaced in 3-A.

## Rules
Delete only after real caller count is zero. Do not rename compatibility and keep it. Preserve unrelated connection/pairing route values when they represent real connection-domain intent.

## Acceptance
- [ ] zero product callers for deleted types;
- [ ] generated bindings/client contain no stale fields;
- [ ] no fallback restores pre-refactor routing.

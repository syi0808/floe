# 3-C — Protocol and FFI Contract Cutover

**Status: ready**

**Execution baseline:** `25b1ccf0f4a4a51d3ef9493a4b510143a4c08f31` (`refactor: close Stage 3-B AppHost composition`)

This document is the authoritative execution plan for 3-C.

## Goal

Define and implement the final **Rust protocol + FFI product contracts** for the Stage 3 connection/pairing and remote-access flows.

3-C removes product-wire model/source topology from those flows. It does **not** migrate Flutter callers yet; 3-D consumes the Rust contract frozen here.

After 3-C:

- canonical Conversation stays on `command_v2/query_v2/events_v2` with the existing intent-only DTO;
- the obsolete AgentVault ConversationTurn wire is gone;
- `AgentRemoteRouteDto` is gone from the final Rust protocol;
- initial pairing may name a bounded loopback setup endpoint because no durable connection exists yet;
- pairing requests do not carry bearer/model-purpose/consent/source-catalog fields;
- after pairing, authority/grant operations receive no endpoint, bearer or pairing bundle from the product caller;
- post-pairing protected operations resolve the current saved connection inside Rust and bind it to verified `CallerContext` person/device identity;
- pairing/Connections meaning is separate from Access grant meaning;
- FFI does structural validation/conversion only;
- Rust protocol/FFI intentionally no longer accepts the old Flutter route-shaped requests. 3-D updates those callers.

This is a **direct contract replacement**, not a compatibility-version exercise.

---

## Frozen foundation

Do not reopen or weaken:

- Stage 2 Conversation Run/replay/recovery semantics;
- Stage 2 exact recipient, current-authority and post-response revalidation;
- Stage 2 durable side-effect and cancellation invariants;
- 3-A canonical Inference execution;
- 3-A canonical Foundation transport;
- 3-A Expert/Schedule/Learner convergence;
- 3-B host-scoped `CurrentSavedConnectionStore`;
- 3-B provider-owned current credential loading/admission;
- 3-B canonical AppHost `ConversationCommands` / `ConversationQueries` / `ConversationEvents`;
- Connections pairing identity/challenge/report validation;
- Access producer pinning, remote grant scope, authority and revocation semantics;
- provider-owned credential redaction.

Do not route product connection operations through Inference merely because the old DTO was named a route.

---

# Fixed 3-C design

## 1. Canonical Conversation contract is already final

Keep:

~~~text
AppCommandDto::ConversationStartTurn {
  session_id
  expected_revision
  text
  mode
  profile
  retry_of
}
~~~

Do not add:

- `person_id`;
- `device_id`;
- endpoint/base URL;
- bearer/token;
- `remote_route`;
- provider placement;
- Access flags.

Verified person/device remain `AppHost::request(...)` → `CallerContext`.

### Important distinction

The obsolete **wire** path may be deleted:

~~~text
AgentVaultActionDto::ConversationTurn
AgentConversationTurnRequestDto
conversation_turn_request(...) FFI conversion
~~~

The internal:

~~~text
WorkerAction::ConversationTurn
VaultBridge::start_conversation
ConversationTurnRequest
~~~

is still the canonical App runtime machinery behind `ConversationCommands` and must remain.

Likewise, `AgentVaultActionDto::ConversationSession` is still used by current Flutter session start/resume/load/recover code. Do not delete it as collateral damage.

## 2. Product identity must come from AppHost

3-B made current-connection admission require exact verified person/device identity.

The old `floe_core_agent_vault` request has a caller-supplied `person_id` but no trusted host device identity. It cannot be the final security boundary for post-pairing protected operations.

New/final connection and remote-access FFI paths must:

~~~text
request_id
  → AppHost::request(request_id)
  → CallerContext { verified person, verified device, runtime epoch }
  → typed App service
  → owner
~~~

A product request must not supply an alternate person/device that Rust then trusts.

If a DTO contains an identity that is intrinsically part of a signed producer/challenge/source reference, that identity is evidence to validate, not a replacement for `CallerContext`.

## 3. Pairing setup and saved connection are different states

### Before pairing

No saved bearer exists yet.

The user-entered loopback server address is legitimate **connection-setup intent**. It is not a model route.

A pairing setup target therefore contains only the minimum needed to reach the pending server, conceptually:

~~~rust
pub struct PairingTargetDto {
    pub base_url: String,
}
~~~

The exact type name may differ, but it must not contain:

- bearer/token;
- model purpose;
- model placement;
- external-transfer consent;
- recipient;
- connector/calendar catalog;
- duplicate person/device.

The provider `HttpRemoteControl` continues to validate the endpoint as an allowed loopback setup target.

### After pairing

Flutter persists the approved connection to the Floe-owned Keychain slot.

All later protected authority/grant operations use:

~~~text
CurrentSavedConnectionStore
  → provider-owned exact person/device admission
  → opaque authority/source transport
~~~

The caller sends owner intent only.

## 4. Pairing identity is derived, not route-carried

For pairing confirm/status/finalize:

- `person_id` = verified `CallerContext.person_id`;
- `device_id` = verified `CallerContext.device_id`;
- `client_id` / pairing identity = the exact pending `pairing_id` named by the signed challenge/operation;
- endpoint = bounded pairing setup target.

Connections remains responsible for validating that challenge/status/report identity is exact.

Do not add a generic `pairing: { person_id, device_id, client_id }` wire object solely to preserve the old shape.

## 5. Pairing token output is a bounded exception

An **approved pairing result** may legitimately contain the newly issued token long enough for the product caller to persist it in the secure credential store.

This is allowed:

~~~text
pairing finalize response
  → newly issued token
  → Keychain persistence
~~~

This is not allowed:

~~~text
saved token
  → every later Rust authority/grant request
~~~

Requirements:

- token appears only on the approved pairing result shape where required;
- non-approved statuses cannot carry it;
- Rust `Debug`/diagnostics for token-bearing DTOs must redact or omit it;
- tests must prove the token is absent from debug/error/trace output;
- 3-D will update Flutter to persist the token and stop resending it.

Do not remove the model-consent fields from the **saved server connection** itself; Inference still owns model-recipient consent. They simply do not belong in pairing/Access request DTOs.

## 6. Remote authority is Access intent, not model route intent

Post-pairing authority operations:

- inspect producer;
- review/enroll producer;
- enrollment status;

must use the current saved connection internally.

Conceptual flow:

~~~text
typed Remote Access service
  → CallerContext person/device
  → provider current authority transport
  → Access RemoteAuthorityStore / policy
~~~

No product request needs:

- base URL;
- bearer;
- `purpose`;
- `external`;
- `allow_external`;
- `recipient`;
- model route;
- connector catalog.

Provider adapters may add a store-backed `RemoteAuthorityEndpoint::from_current_connection(...)` or equivalent. App must not load the raw saved connection itself.

## 7. Remote grants are Access intent, not connection transport bundles

Post-pairing Calendar/View grant operations must speak in Access-owned source/reference/review terms.

Calendar:

- preview: connector, connection, resource;
- review: same + expected producer fingerprint;
- status/pause: grant identity + expected authority where required.

Remote view:

- preview: view, connector, connection, resource, consumer;
- review: same + exact expected producer/source/revision/provider/recipient facts;
- status/pause: grant identity + expected authority.

Transport/pairing identity is resolved from the current saved connection and validated against `CallerContext`.

`calendar_connections` must not be carried in a generic request. Source catalogs remain lazy observations via provider/Connections.

## 8. Typed App services define the final product boundary

Follow the 3-B Conversation service pattern.

Add narrow App service contracts for the product flows, conceptually:

~~~text
RemotePairingCommands
  prepare
  confirm
  status
  finalize

RemoteAccessCommands
  inspect producer
  review/enroll producer
  enrollment status
  calendar grant preview/review/status/pause
  view grant preview/review/status/pause
~~~

Exact names may differ if a cleaner existing naming convention emerges, but:

- every method accepts `&CallerContext`;
- caller identity is not duplicated in the request;
- App request values carry owner intent only;
- App delegates owner decisions to Connections/Access;
- secret-bearing transport preparation stays in provider adapters.

Do not make FFI call `VaultBridge`/`WorkerAction` directly for these final paths.

Internal Worker actions may carry the verified device ID supplied by the typed App service if required to execute asynchronously. That is trusted runtime context, not wire identity.

## 9. Protocol modules should reflect owner domains

Do not add another generic route-shaped file.

Prefer protocol organization such as:

~~~text
dto/connections.rs
  pairing setup/request/result values

dto/access.rs
  remote authority/grant request/result values
~~~

or an equivalent owner-oriented split.

The final DTO names should make it impossible to confuse:

- pairing endpoint;
- saved connection;
- model route;
- Access grant/source reference.

Keep `APP_WIRE_VERSION = 2` and `PROTOCOL_VERSION = 1` unless the checkout already differs for an unrelated committed reason.

Do not create `_v3`, `next`, legacy decoders or dual route formats.

## 10. FFI remains structural

New/final handlers may use dedicated typed FFI entry points or a clearly typed owner request surface. The exact ABI symbol naming can follow the repository's existing `_v2` convention.

Requirements are semantic, not cosmetic:

- one request envelope carries schema version + request ID + owner operation;
- handler calls `AppHost::request(request_id)`;
- operation converts to typed App service values;
- caller identity comes from `HostRequest::caller()`;
- FFI performs bounds/ID/enum/JSON conversion;
- FFI does not authorize grants, compare recipients, choose transport, load Keychain, or select models.

Update `crates/bindings/ffi/include/floe_ffi.h` in the same snapshot.

`floe_core_command_v2/query_v2/events_v2` remain canonical and unchanged.

## 11. Direct replacement, not dual contract preservation

The repository's refactor policy explicitly does not require old local API compatibility.

Therefore 3-C should directly remove the obsolete Rust route-shaped request contract once the final typed Rust contract exists.

It is acceptable and expected that, between 3-C and 3-D:

~~~text
Rust protocol/FFI = final contract
Flutter sender code = still old contract
~~~

3-D exists to close that caller mismatch.

Do **not** preserve `AgentRemoteRouteDto` or old remote AgentVault variants as aliases solely so old Flutter can continue to work for one checkpoint.

Flutter source itself is not modified in 3-C.

---

# C0 — Baseline and exact caller inventory

Before editing:

- [ ] confirm HEAD descends from `25b1ccf0f4a4a51d3ef9493a4b510143a4c08f31`;
- [ ] confirm 3-A and 3-B remain `complete · frozen`;
- [ ] record pristine Rust warning/test baseline;
- [ ] record all exact callers below.

Run at minimum:

~~~sh
rg -n \
  'AgentConversationTurnRequestDto|AgentVaultActionDto::ConversationTurn|conversation_turn_request' \
  crates apps --glob '*'

rg -n \
  'AgentRemoteRouteDto|RemoteTurnRoute|RemoteRoute|RoutePairing|remote_route\(' \
  crates apps --glob '*'

rg -n \
  'RemoteAuthorityInspectProducer|RemoteAuthorityReviewAndEnroll|RemoteAuthorityEnrollmentStatus|RemotePairingConfirm|RemotePairingStatus|RemotePairingFinalize|RemoteCalendarGrantPreview|RemoteCalendarGrantReview|RemoteViewGrantPreview|RemoteViewGrantReview' \
  crates apps --glob '*'

rg -n \
  'authorityRoute\(|pairingRoute\(|bearer_token|calendar_connections' \
  apps/client crates/bindings crates/app --glob '*'
~~~

Classify each match:

1. canonical/final Rust path;
2. obsolete wire path to delete in 3-C;
3. Flutter caller intentionally handed to 3-D;
4. low-level provider/internal model path unrelated to product wire;
5. test-only;
6. zero caller.

Do not delete `RemoteRoute`/`RoutePairing` globally merely because the product wire no longer uses them.

---

# C1 — Remove obsolete AgentVault ConversationTurn wire

Primary files:

- `crates/bindings/protocol/src/dto/agent.rs`;
- `crates/bindings/protocol/src/dto/mod.rs`;
- `crates/bindings/protocol/src/lib.rs`;
- `crates/bindings/protocol/tests/*`;
- `crates/bindings/ffi/src/conversion/worker.rs`.

## C1.1 Prove product caller zero

Search Flutter production code for an actual operation payload:

~~~text
kind = conversation_turn
~~~

Do not confuse:

- error metadata string `conversation_turn`;
- Dart class names such as `AgentConversationTurnRequest`;
- internal Rust `WorkerAction::ConversationTurn`;
- canonical `runConversationTurn()` methods backed by AppWire.

Expected production sender count: zero.

## C1.2 Delete wire-only shapes

Delete:

- [ ] `AgentVaultActionDto::ConversationTurn`;
- [ ] `AgentConversationTurnRequestDto`;
- [ ] its `remote_route` field;
- [ ] FFI `conversation_turn_request(...)`;
- [ ] protocol exports/tests existing only for the old turn wire;
- [ ] AgentVault conversion match arm for the old turn action.

Keep:

- [ ] `WorkerAction::ConversationTurn`;
- [ ] `ConversationTurnRequest`;
- [ ] canonical `ConversationCommands`;
- [ ] `AppCommandDto::ConversationStartTurn`;
- [ ] `AgentVaultActionDto::ConversationSession` while its Flutter caller remains.

A canonical AppWire negative test may assert that route/bearer fields are unknown/rejected, without retaining the old DTO.

### C1 static gate

~~~sh
rg -n \
  'AgentConversationTurnRequestDto|AgentVaultActionDto::ConversationTurn|conversation_turn_request' \
  crates/bindings --glob '*.rs'
~~~

Expected: no match.

---

# C2 — Define final pairing setup contract

Primary areas:

- protocol owner DTO module(s);
- `crates/app/src/services.rs`;
- `crates/app/src/composition.rs`;
- `crates/app/src/vault_host.rs`;
- `crates/app/src/worker.rs`;
- provider `control/authorization.rs`;
- Connections pairing tests.

## C2.1 Pairing target

Define a bounded pairing target containing only the loopback setup endpoint.

Example semantic shape:

~~~rust
pub struct PairingTarget {
    pub base_url: String,
}
~~~

Validation:

- non-empty/bounded text at protocol/App layer;
- exact URL/loopback semantics remain provider transport responsibility;
- no bearer;
- no model purpose/placement/recipient;
- no source catalog;
- no duplicate person/device.

Do not rename `AgentRemoteRouteDto` to `PairingRouteDto`.

## C2.2 Typed App pairing service

Define owner-intent request values for:

- prepare owner key;
- confirm pairing;
- read pairing status;
- finalize pairing.

Each method receives verified `CallerContext`.

For confirm:

~~~text
CallerContext person/device
+ pairing target endpoint
+ signed challenge
+ polling proof
→ Connections confirm_pairing
~~~

For status/finalize:

~~~text
CallerContext person/device
+ pairing target endpoint
+ pairing_id
+ polling proof
(+ challenge for finalize)
→ Connections
~~~

Derive `PairingIdentity` from trusted caller identity + exact pairing ID. Do not accept caller-provided identity as authority.

## C2.3 Preserve pairing owner invariants

Tests must retain/prove:

- challenge pairing ID must match;
- Person exact match;
- device exact match;
- owner-key issuer exact match;
- signed challenge verification;
- stale/foreign pairing denied before unsafe settlement;
- cancellation/deadline;
- approved versus rejected/expired state semantics.

No model or Access grant policy belongs in the pairing service.

## C2.4 Pairing token output hygiene

Keep token only on approved pairing result if needed for secure persistence.

Update DTO/debug behavior so:

- serialization can return the token to the intended caller;
- `Debug` never prints it;
- diagnostic/error metadata never includes it;
- non-approved result carrying token is invalid.

Add explicit regression tests.

---

# C3 — Store-backed remote authority contract

Primary areas:

- App typed remote-access service;
- `crates/app/src/vault_host.rs`;
- `crates/app/src/vault_host/remote_authority.rs`;
- provider `control/authorization.rs`;
- Access remote-authority tests.

Operations:

- inspect producer;
- review and enroll producer;
- enrollment status.

## C3.1 No route input after pairing

Final request shapes carry only operation intent, e.g.:

~~~text
inspect producer
review/enroll { producer }
enrollment status { enrollment_id }
~~~

They do not carry endpoint/bearer/pairing/model fields.

## C3.2 Prepare authority transport from current connection

Add/reuse a provider-owned store-backed constructor, semantically:

~~~text
CurrentSavedConnectionStore
+ verified person/device
→ admitted current connection
→ RemoteAuthorityEndpoint
~~~

App must not:

- call `store.load()` for the raw connection;
- read base URL/token;
- construct `RemoteRoute`;
- construct `PreparedServerSource::from_parts` from wire values.

Provider validates exact saved identity and keeps credential private.

## C3.3 Pairing identity for Access

Where Access requires `RemotePairingIdentity`, derive it from:

- verified caller person/device;
- admitted current connection client ID.

Reject a saved connection whose identity does not exactly match the caller.

Preserve producer pinning and owner-key semantics.

---

# C4 — Store-backed remote grant contract

Primary areas:

- typed App remote-access service;
- worker/App remote grant execution;
- provider source/authority adapters;
- Access remote calendar/view grant tests;
- Context remote source validation where directly affected.

## C4.1 Calendar grants

Final request intent:

~~~text
preview {
  connector_id
  connection_id
  resource
}

review {
  connector_id
  connection_id
  resource
  expected_producer_fingerprint
}

status {
  grant_id
}

pause {
  grant_id
  expected_authority
}
~~~

No route, bearer, purpose, model consent or calendar catalog.

Use the current admitted connection internally for:

- provider transport;
- exact pairing identity;
- producer/source verification.

## C4.2 Remote view grants

Final request intent:

~~~text
preview {
  view_id
  connector_id
  connection_id
  resource
  consumer
}

review {
  view_id
  connector_id
  connection_id
  resource
  consumer
  expected_producer_fingerprint
  expected_source_authority
  expected_connection_revision
  expected_provider_identity
  expected_recipient
}

status {
  grant_id
}

pause {
  grant_id
  expected_authority
}
~~~

The expected fields in review are Access review evidence, not transport configuration. Keep them exact.

## C4.3 Remove source transport reconstruction from wire fields

Delete App execution patterns such as:

~~~text
PreparedServerSource::from_parts(
  route.base_url,
  route.bearer_token,
  route.pairing...
)
~~~

Use `ServerSourceClient::from_current_connection(...)` or an equivalent provider-owned store-backed factory.

Remote grant preview/review must still verify:

- pinned producer;
- source authority;
- connection revision;
- provider identity;
- exact recipient;
- scope/resource/consumer;
- current grant authority.

## C4.4 `calendar_connections` route field is dead

Do not migrate `calendar_connections` into any replacement DTO.

The real connector/source catalog remains:

~~~text
ServerSourceClient::observe_calendar_connections
→ Connections projection
~~~

and stays lazy.

---

# C5 — Remove route-shaped Rust product contract

Primary files:

- `crates/bindings/protocol/src/dto/agent.rs`;
- protocol exports;
- `crates/bindings/ffi/src/conversion/worker.rs`;
- `crates/app/src/turn_request.rs`;
- `crates/app/src/worker.rs`;
- `crates/app/src/lib.rs`;
- affected App/FFI/protocol tests.

## C5.1 Delete `AgentRemoteRouteDto`

After C2-C4 are wired:

- [ ] delete `AgentRemoteRouteDto`;
- [ ] delete `AgentRemotePairingDto` if no independent final wire use remains;
- [ ] delete `AgentRemoteCalendarConnectionDto` if its only caller was the route;
- [ ] delete `remote_route(...)` FFI conversion;
- [ ] remove all route fields from remote authority/pairing/grant AgentVault actions.

Do not leave an old decoder that silently accepts these fields.

## C5.2 Remove old AgentVault remote route variants

The old route-carrying AgentVault operations are not kept as aliases for 3-D.

Once final typed Rust owner APIs exist, remove their obsolete protocol/FFI conversion variants for:

- remote authority inspect/review/status;
- pairing confirm/status/finalize;
- remote calendar grant preview/review;
- remote view grant preview/review.

Status/pause operations that already carry owner intent may be moved into the new typed Access contract rather than preserved as duplicate AgentVault variants.

`RemotePairingPrepare` likewise moves to the final pairing contract.

Do not broadly delete unrelated AgentVault operations still used by Flutter.

## C5.3 Replace `RemoteTurnRoute` inside App

Refactor internal Worker actions for the migrated operations so they carry:

- trusted runtime device identity passed from typed service;
- pairing setup endpoint only for pre-save pairing operations;
- operation-specific owner intent.

Post-pairing actions carry no endpoint/token.

Re-run callers for:

- `RemoteTurnRoute`;
- App root re-export;
- `.pairing()` / `.recipient()`;
- `floe_inference::RoutePairing`;
- `floe_inference::RemoteRoute`.

If `RemoteTurnRoute` becomes caller-zero outside tests after this cutover, delete it in 3-C as direct fallout.

Do **not** delete Inference internal/provider route values unless their own caller count is zero.

## C5.4 Worker actions are internal, not wire models

It is acceptable for App's worker queue to keep internal operation variants corresponding to pairing/grants.

They must use owner-shaped fields and trusted runtime identity, not wire route bundles.

Do not mirror the final protocol DTO structs directly inside the worker just to avoid conversion.

---

# C6 — Protocol and FFI typed entry points

Primary files:

- protocol DTO modules/exports/tests;
- `crates/bindings/ffi/src/abi.rs`;
- `crates/bindings/ffi/src/bridge.rs` only if needed;
- new typed FFI conversion/handler modules as appropriate;
- `crates/bindings/ffi/include/floe_ffi.h`;
- `crates/bindings/ffi/tests/c_abi.rs`.

## C6.1 Protocol request envelopes

Use `APP_WIRE_VERSION = 2`.

Define owner-specific request/result envelopes containing:

- schema version;
- request ID;
- operation.

Do not include caller person/device.

Recommended separation:

~~~text
Connections / Pairing request
Remote Access request
~~~

Exact public type names are implementation detail; owner meaning is not.

All input DTOs:

- `deny_unknown_fields`;
- validate collection/text bounds;
- parse IDs structurally;
- do not accept a secret route fallback.

## C6.2 FFI handler pattern

Use the canonical pattern:

~~~text
JSON decode
→ request.validate()
→ handle.app().request(request_id)
→ typed App service
→ result DTO
→ AppResponse envelope
~~~

Do not call `handle.services()` for the final new APIs.

Do not call `VaultBridge::request(...)` directly from FFI.

## C6.3 ABI/header

Expose the new typed product entry points in `abi.rs` and `floe_ffi.h`.

Names may follow the current `_v2` convention. Do not bump the version just because a new owner endpoint is added.

Add C ABI tests proving:

- symbol exists;
- malformed JSON fails structurally;
- request ID correlation survives;
- unsupported schema fails correctly;
- verified host identity is used;
- route/bearer fields are rejected.

## C6.4 Do not alter canonical Conversation AppWire

`floe_core_command_v2`, `floe_core_query_v2`, `floe_core_events_v2` remain unchanged except shared helper refactors that are behavior-neutral.

---

# C7 — Compatibility inventory and 3-D handoff

After C1-C6, classify remaining outer APIs.

Expected to remain for later caller cutover may include:

- `floe_core_agent_vault` for unrelated live AgentVault operations;
- AgentFixture ABI/runtime;
- `floe_core_agent_fixture[_run]`;
- old Day/Calendar/local-context/action typed APIs;
- `AppHost::legacy_services()` / `FloeHandle::services()` while those callers remain;
- Flutter `authorityRoute()` / `pairingRoute()` and route-taking gateway method signatures — **unchanged in 3-C source**, now stale callers for 3-D.

The 3-C report must list exact Flutter files/callers that 3-D must migrate.

Do not keep Rust old route DTOs merely because those Flutter callers still exist.

### Expected 3-D caller list

At minimum revalidate:

- `apps/client/lib/features/connections/application/local_server_client.dart`;
- `apps/client/lib/features/connections/presentation/local_server_panel.dart`;
- `apps/client/lib/features/connections/presentation/server_connector_panel.dart`;
- `apps/client/lib/app/runtime/agent_vault_gateway.dart`;
- pairing operation gateway code;
- related Flutter tests.

Record the exact method signatures/JSON fields that are now stale.

---

# C8 — Validation and closure

## Focused Rust crates

Run at minimum:

~~~sh
cargo check -p floe-protocol
cargo test -p floe-protocol

cargo check -p floe-ffi
cargo test -p floe-ffi

cargo check -p floe-app
cargo test -p floe-app

cargo check -p floe-connections
cargo test -p floe-connections

cargo check -p floe-access
cargo test -p floe-access

cargo check -p floe-provider-adapters
cargo test -p floe-provider-adapters
~~~

If Context or another owner crate changes, add its focused check/tests.

## Full Rust gate

~~~sh
cargo check --workspace --lib
cargo test --workspace --no-fail-fast
python3 tools/architecture/check_boundaries.py
git diff --check
~~~

Required:

- zero failed workspace tests;
- architecture `errors == []`;
- architecture `warnings == []`;
- `git diff --check` clean;
- no new warning without a documented reason.

Flutter source is not modified in 3-C and is not the acceptance source for the new Rust caller contract. 3-D owns Flutter migration/build closure.

## Protocol static gates

~~~sh
rg -n \
  'AgentRemoteRouteDto|AgentConversationTurnRequestDto|conversation_turn_request|remote_route\(' \
  crates/bindings --glob '*.rs'
~~~

Expected: no match.

Final request DTOs:

~~~sh
rg -n \
  'bearer_token|allow_external|calendar_connections' \
  crates/bindings/protocol/src --glob '*.rs'
~~~

Any remaining match must be:

- an output or unrelated legitimate contract; or
- a blocker.

There must be no such field in final pairing/remote-access **request** DTOs.

`purpose`, `recipient`, `external` may legitimately appear in Access preview/result evidence or other domains. They must not reconstitute a transport route.

## App/worker static gates

For migrated operations:

~~~sh
rg -n \
  'RemoteTurnRoute|route\.route|route\.pairing\(\)|PreparedServerSource::from_parts' \
  crates/app/src/worker.rs \
  crates/app/src/vault_host.rs
~~~

Expected: no production match attributable to the migrated product operations.

Post-pairing authority/grant execution must show current-store/provider preparation instead.

## Identity gate

Final typed product handlers must not accept a caller identity override:

~~~text
person_id
device_id
```

as top-level authority for the request.

Signed evidence may contain identity fields and pairing results may report identity; those are validated data, not caller selection.

Tests must prove a foreign saved connection or mismatched signed identity fails closed.

## Secret gate

Test:

- final pairing **input** has no bearer;
- final remote authority/grant input has no bearer or endpoint;
- approved pairing **output** token is serializable for secure persistence;
- token is redacted from Debug/diagnostics;
- provider credential remains private after persistence;
- protected post-pairing call reads current connection internally.

## Frozen-runtime regression

Re-run/prove:

- Conversation start turn still works through canonical AppWire;
- exact recipient fencing unchanged;
- source/model catalog separation unchanged;
- no model selection occurs in pairing/access services;
- remote grant revocation/current authority unchanged;
- query/preview calls do not cancel durable Runs.

Known Calendar mock-socket `WouldBlock` handling remains unchanged: reproduce alone and compare to baseline; do not add sleeps/retries or weaken assertions. Final full Rust workspace run must be green.

---

# Explicit non-goals

Do not in 3-C:

- edit Flutter production code;
- migrate Flutter method signatures;
- change Swift/native code;
- change server HTTP endpoint semantics unless a Rust test reveals an actual final-contract incompatibility that cannot be expressed at the adapter boundary;
- change model profile routing;
- remove saved model external-consent state;
- delete AgentFixture runtime;
- delete all AgentVault operations;
- delete `ConversationSession` while its live caller remains;
- remove `AppHost::legacy_services()` merely because new APIs no longer need it;
- bump `APP_WIRE_VERSION` or `PROTOCOL_VERSION`;
- create a `v3`, `next`, legacy decoder or route alias;
- preserve `AgentRemoteRouteDto` just to make old Flutter callers continue working before 3-D.

---

# 3-C acceptance

3-C is complete only when all are true:

- [ ] canonical Conversation AppWire shape is unchanged and remains intent-only;
- [ ] obsolete AgentVault ConversationTurn wire is deleted while internal `WorkerAction::ConversationTurn` remains;
- [ ] final pairing input contains only setup endpoint + operation-specific pairing evidence;
- [ ] pairing identity derives from `CallerContext` + exact pairing ID/challenge;
- [ ] approved pairing may return the newly issued token, with Debug/diagnostic redaction;
- [ ] post-pairing authority operations take no endpoint/bearer/route input;
- [ ] remote Calendar/View grant operations take no endpoint/bearer/model-route input;
- [ ] provider adapters prepare authority/source transports from the host-scoped current store;
- [ ] exact person/device binding is preserved;
- [ ] `AgentRemoteRouteDto` is deleted from Rust protocol;
- [ ] `calendar_connections` is not copied into a replacement product request;
- [ ] old remote AgentVault route variants/conversions are removed rather than aliased;
- [ ] `RemoteTurnRoute` is deleted if caller-zero after cutover;
- [ ] new typed FFI handlers admit requests through `AppHost::request`;
- [ ] FFI performs structural conversion only;
- [ ] `APP_WIRE_VERSION == 2` and `PROTOCOL_VERSION == 1`;
- [ ] Flutter source is unchanged but exact 3-D stale callers are documented;
- [ ] focused/full/static/security gates are green.

When complete:

1. set this document to `complete · frozen`;
2. append the Agent report below;
3. check 3-C in `docs/refactoring/stage-3.md`;
4. move Current checkpoint to `3-D — Flutter, Native and Server Caller Cutover`;
5. update current architecture docs only if the durable product-boundary description changed;
6. stop. Do not start 3-D in the same change set.

---

# Agent report format

Report only:

1. execution baseline and changed files/symbols by C0-C8;
2. obsolete ConversationTurn wire caller proof and deletion result;
3. final pairing request/result contract;
4. AppHost verified identity derivation for pairing;
5. approved pairing-token output/redaction proof;
6. final remote-authority contract and current-connection preparation;
7. final remote Calendar/View grant contract and current-connection preparation;
8. `AgentRemoteRouteDto` / `RemoteTurnRoute` / route-conversion deletion or exact retained-caller classification;
9. typed App service and FFI/ABI/header surface;
10. retained unrelated AgentVault/AgentFixture/legacy-services compatibility with later owner checkpoint;
11. exact Flutter 3-D stale caller list;
12. protocol/App/identity/secret static gates;
13. focused tests;
14. workspace/architecture/`git diff --check`;
15. warning/flaky baseline result if applicable;
16. remaining 3-C blocker, if any;
17. final document/checkpoint state.

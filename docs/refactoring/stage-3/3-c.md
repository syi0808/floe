# 3-C — Protocol and FFI Contract Cutover

**Status: pending**

## Goal
Make product wire contracts express user intent rather than internal execution topology.

## Checklist
- [ ] inventory route/provider/internal-policy fields in protocol/FFI DTOs;
- [ ] define final Conversation intent DTO: session/revision/text/mode/profile intent/retry;
- [ ] remove caller-provided endpoint/base URL/bearer/model route;
- [ ] keep explicit profile selection only when it is user intent;
- [ ] separate connection/pairing commands from model execution route data;
- [ ] update Rust conversion and generated bindings together;
- [ ] keep ABI conversion free of business policy;
- [ ] remove deprecated DTO variants after caller cutover.

Primary files: `crates/bindings/protocol/src/dto/agent.rs`, `crates/bindings/ffi/src/conversion/worker.rs`.

## Acceptance
- [ ] product wire cannot choose arbitrary model endpoint;
- [ ] no model bearer/token in product wire;
- [ ] `AgentRemoteRouteDto` model-execution usage = 0.

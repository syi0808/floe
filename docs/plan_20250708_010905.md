# Development Plan 2025-07-08 01:09 UTC

Add unit tests covering remaining MCPClient endpoints.
1. Mock POST requests for `send_reply` and `send_notification` under `tests/mcp`.
2. Verify payload construction and HTTP method usage.
3. Ensure non-200 responses raise `HTTPError`.

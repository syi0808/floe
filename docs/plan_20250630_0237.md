# Development Plan 2025-06-30 02:37 UTC

- Ensure health agent modules and tests are functioning.
- Install project dependencies from `requirements.txt`.
- Run the full test suite with `pytest`.
- Investigate failing inbox agent tests and adjust code if necessary.
- Commit changes once all tests pass.

## Goals
- Finalise CRUD features and priority logic in `task_agent/task_core.py`.
- Complete time blocking integration in `task_agent/task_calendar_linker.py` by providing a method that coordinates with `ScheduleAgent` and calendar adapters.
- Ensure tests under `tests/task_agent/` cover new functionality.

## Tasks
1. Review existing CRUD logic in `task_core.py` and adjust if necessary.
2. Implement `TaskCalendarLinker.block_time_for_task` which:
   - Calls `block_time_for_task_via_schedule_agent` to obtain an event id.
   - Creates the calendar event using the adapter and returned id.
   - Returns the final event id.
3. Update unit tests to exercise the new method.
4. Run `pytest tests/task_agent` to confirm all TaskAgent tests pass.


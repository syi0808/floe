---
id: codex_task_20250701_105706
author: codex
date: 2025-07-01T10:57:06+00:00
model_context: "python3.10"
task_type: feature
priority: P1
linked_issues: []
---

## 1. 문제 설명

InsightGenerator의 요약을 세션 간 유지하기 위해 선택적으로 디스크 캐시를 지원한다.

## 2. 입력 맥락

* **파일/모듈**: `insight_agent/insight_generator.py`
* **테스트**: `tests/insight_agent/test_insight_generator.py`

## 3. 기대 결과 / 성공 기준

| # | 성공 조건 | 검증 방법 |
| - | ----- | ---------- |
| 1 | cache_path 지정 시 캐시가 유지됨 | pytest |
| 2 | JSON과 SQLite 파일 모두 지원 | pytest |

## 4. 제약 사항

* 표준 라이브러리만 사용할 것
* cache_path가 None일 때 기존 동작 유지

## 5. 단계별 계획

1. `InsightGenerator.__init__`에 `cache_path` 인자를 추가하고 캐시 로드
2. JSON과 SQLite 핸들러를 구현하고 업데이트 시 플러시
3. compile 및 clear_cache 수정
4. 인스턴스 간 지속성을 검증하는 테스트 작성
5. 문서 작성 후 `pytest` 실행

## 7. 테스트 / 확인 항목

```python
path = tmp_path / "cache.json"
InsightGenerator(cache_path=str(path)).compile(sample_data())
assert path.exists()
```

## 8. 생성 후 확인

* [ ] `pytest` 통과

## 9. 참고 자료

* 없음

## 10. 변경 이력

| 버전 | 날짜 | 변경 내역 |
| --- | ------------ | ----- |
| 0.1 | 2025-07-01 | 최초 작성 |

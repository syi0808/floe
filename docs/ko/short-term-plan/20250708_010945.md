---
id: codex_task_20250708_010945
author: codex
date: 2025-07-08T01:09:45+00:00
model_context: "python3.10"
task_type: feature
priority: P1
linked_issues: []
---

## 1. 문제 설명

웹 UI 보안을 강화하고 오케스트레이터 오류 처리 방식을 개선합니다.

## 2. 입력 맥락

* **파일/모듈**: `floe_ui/server.py`, `floe_ui/templates/index.html`, `README.md`
* **테스트**: `tests/floe_ui/test_auth.py`

## 3. 기대 결과 / 성공 기준

| # | 성공 조건 | 검증 방법 |
| - | ----- | -------- |
| 1 | `/query` 호출 시 토큰 필요 | TestClient 활용한 pytest |
| 2 | 오류 발생 시 로깅되고 구조화된 JSON 반환 | pytest |

## 4. 제약 사항

* 환경 변수 기반 단일 토큰 방식 사용

## 5. 작업 단계

1. `server.py`에 토큰 검증 의존성 구현
2. 토큰 미사용 또는 잘못된 경우 401 반환
3. `engine.route_request` 오류를 try/except로 감싸고 로그 기록 후 JSON 오류 응답
4. 템플릿에 토큰 입력란 추가 및 Authorization 헤더 전송
5. README에 토큰 설정 방법 기술
6. 인증 및 오류 처리 테스트 추가
7. 영어/한국어 개발 기록 작성
8. `pytest` 실행

## 6. 코드 스켈레톤

```python
# server.py
AUTH_TOKEN = os.getenv("FLOE_UI_TOKEN", "changeme")
async def verify_token(request: Request): ...
```

## 7. 테스트 / 확인 항목

```python
client.post("/query", json=..., headers={"Authorization": f"Bearer {AUTH_TOKEN}"})
```

## 8. 후속 체크리스트

* [ ] `pytest` 통과

## 9. 참고 문헌

* 없음

## 10. 변경 이력

| 버전 | 날짜 | 변경 내역 |
| --- | --- | --- |
| 0.1 | 2025-07-08 | 최초 작성 |

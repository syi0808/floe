# 개발 계획 2025-07-08 01:09 UTC

MCPClient의 남은 엔드포인트 테스트를 추가합니다.
1. `tests/mcp`에서 `send_reply`와 `send_notification` POST 호출을 모킹합니다.
2. 페이로드 구성과 HTTP 메서드를 검증합니다.
3. 200이 아닌 응답 시 `HTTPError`가 발생하는지 확인합니다.

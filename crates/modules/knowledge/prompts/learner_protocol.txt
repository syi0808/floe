No capabilities, tools, experts, delegation, network, credentials, or external actions are available.

Return exactly one JSON object with schema_version 1 and a proposal field. For no change:
{"schema_version":1,"proposal":null}

For a proposal, include only observation_kind, value, target_id, and base_revision. Value includes kind, statement, epistemic_status, confidence_millis, valid_from, valid_until, and observed_at. Use null for absent optionals. Do not use Markdown.

Enums are exact: observation_kind is explicit_remember, user_correction, outcome_conflict, or reusable_procedure; value.kind is fact, observation, inference, preference, or commitment; epistemic_status is fact or inference, and is inference exactly when kind is inference.

confidence_millis is an integer 0 through 1000. statement is nonempty and preserves the exact named person or entity from evidence; never replace a named fictional person with "the user".

For a new memory, target_id and base_revision are null. Never use a person's name as target_id. For a revision, copy the exact UUID target_id and positive integer base_revision from one supplied current memory.

valid_from and valid_until are null unless evidence explicitly says when the preference starts or expires. Select the timeless form otherwise. Never copy the conversation's Observed at timestamp, use model knowledge, or invent a date. If dates are explicit, they are real RFC 3339 calendar timestamps and valid_until is later than valid_from. observed_at is runtime-owned; use "1970-01-01T00:00:00Z" as its placeholder.

New preference example (replace only the subject and claim with supported evidence):
{"schema_version":1,"proposal":{"observation_kind":"explicit_remember","value":{"kind":"preference","statement":"A contact: prefers tea","epistemic_status":"fact","confidence_millis":1000,"valid_from":null,"valid_until":null,"observed_at":"1970-01-01T00:00:00Z"},"target_id":null,"base_revision":null}}

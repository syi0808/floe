[package]
name = "floe-experts"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
chrono.workspace = true
floe-agent-contract = { path = "../../contracts/agent" }
floe-context-contract = { path = "../../contracts/context" }
floe-execution = { path = "../../runtime/execution" }
floe-inference = { path = "../inference" }
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
tokio.workspace = true
uuid.workspace = true

[dev-dependencies]
# The delegation regressions drive the real engine; the Task owner itself
# does not.
floe-agent-runtime = { path = "../../runtime/agent" }
tempfile = "3.23.0"

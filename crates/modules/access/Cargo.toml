[package]
name = "floe-access"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
base64 = "0.22.1"
chrono.workspace = true
floe-context-contract = { path = "../../contracts/context" }
floe-execution = { path = "../../runtime/execution" }
floe-kernel = { path = "../../contracts/kernel" }
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
tokio.workspace = true
uuid.workspace = true

[dev-dependencies]
# The lease registry is Context's; the regressions here check that an
# admission and a real lease agree.
floe-context = { path = "../context" }

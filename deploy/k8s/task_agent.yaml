apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: task-agent
  template:
    metadata:
      labels:
        app: task-agent
    spec:
      containers:
        - name: task-agent
          image: task-agent:latest
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: task-agent
spec:
  selector:
    app: task-agent
  ports:
    - port: 8000
      targetPort: 8000

apiVersion: apps/v1
kind: Deployment
metadata:
  name: health-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: health-agent
  template:
    metadata:
      labels:
        app: health-agent
    spec:
      containers:
        - name: health-agent
          image: health-agent:latest
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: health-agent
spec:
  selector:
    app: health-agent
  ports:
    - port: 8000
      targetPort: 8000

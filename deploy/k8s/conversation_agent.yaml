apiVersion: apps/v1
kind: Deployment
metadata:
  name: conversation-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: conversation-agent
  template:
    metadata:
      labels:
        app: conversation-agent
    spec:
      containers:
        - name: conversation-agent
          image: conversation-agent:latest
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: conversation-agent
spec:
  selector:
    app: conversation-agent
  ports:
    - port: 8000
      targetPort: 8000
